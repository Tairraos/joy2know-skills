# 晓得 · joy2know

**WorkBuddy 开放平台资产包仓库** —— 14 个技能（Skill）、2 个单专家、2 个专家团。

母品牌「晓得」，口号「一目晓得」。中文展示名写在 `display_name`（晓得·讲明白）；**凡需要英文名的位置，「晓得」与「晓得乐」一律译作 `joy2know`** —— 技能看 `display_name_en`，专家看 `profession.en` / `displayName.en`，包名与引用路径同样走 `joy2know-xxx`。
**触发匹配完全靠 `description`，`name` 不参与触发。**

- 作者署名：晓得乐 / joy2know
- 许可协议：**CC BY 4.0**（署名 4.0 国际）—— 可自由转载、改编、商用，**惟须署名**。全文见 [`LICENSE`](LICENSE)
- 归档目录 `private/` 记录的是这批资产的完整诞生过程（提示词 → 回答 → 产物）

---

## 目录结构

```
joy2know-skills/
├── AGENT.md                ★ 工作手册：发布流程 + 发布前检测四道门（AI 和人共用）
├── docs/平台资产规范.md      ★ 完整规范手册：字段 / 结构 / 体积 / 类目 / 许可（原个人技能知识，已固化进仓库）
├── release.config.json     ★ 发布参考单一真源：逐包建议发布类目 + 平台侧最后版本
├── 发布清单.md             ★ 待上传产物一览 + 发布历史（前四节构建时自动重写，末两节人工维护）
├── packages/               ★ 资产包源文件（一个包一个目录，共 18 个）
│   ├── <包名>/skills.json     专家/专家团才有：声明内嵌哪些技能（构建时复制）
│   └── <包名>/<包名>.md       发布前检测报告 —— 开发期文档，**不进 zip**，但进 git
├── avatars/                ★ 图标统一管理（技能 + 专家 + 团队成员，打包时自动注入）
├── dist/                   产物 zip（pnpm build 生成，已 gitignore）
│   └── <包名>-v<版本>.zip     带版本号的发布包（同包只留最新一版）
├── scripts/
│   ├── build.mjs           打包工具
│   ├── selfcheck.py        四道字段自检（**仓库自带，不再依赖本机技能目录**）
│   ├── init-checkdocs.mjs  生成检测报告骨架
│   ├── placeholders.py     图标占位图
│   ├── shrink-icons.py     图标规格体检
│   └── make-avatar.py      零积分画头像
├── private/               阶段归档：流水文档 + 13 份产物 HTML/MD
├── README.md
└── package.json
```

三处分离的用意：**源文件不重复、图标不散落、产物不入库**。

> **本仓库自给自足**：自检脚本、字段规范、打包工具都在仓库里。换一台机器、换一个
> harness 克隆下来就能产出能过平台校验的包，**不依赖任何本机技能目录**。
> 操作流程看 `AGENT.md`，字段口径看 `docs/平台资产规范.md`。

---

## 快速开始

```bash
pnpm build                    # 打包 packages/ 下全部包 → dist/<包名>-v<版本>.zip
pnpm build joy2know-mr        # 只打包指定包（可给多个，支持前缀匹配）
pnpm run list                 # 列出全部包、版本、类型、图标状态与内嵌技能
pnpm run checkdocs            # 查看各包发布前检测报告的状态（缺的补骨架）
python3 scripts/selfcheck.py packages/joy2know-mr   # 单独跑字段自检（不打包）
```

> `pnpm run list` 要带上 `run` —— 裸写 `pnpm list` 会被 pnpm 自带的「列依赖」命令截走。

| 选项 | 作用 |
|---|---|
| `--no-check` | 跳过深度自检（selfcheck.py） |
| `--strict` | 深度自检有问题、或发布前检测报告未通过 / 版本不符，即中止（默认仅告警） |
| `--force` | 即使有错误也写出 zip |
| `--no-skill-icons` | 不把技能图标写进内嵌技能目录 |
| `--keep-old-zips` | 保留 `dist/` 里同包的旧版本 zip（默认清理，只留本次构建的那一版） |
| `--no-manifest` | 不重写仓库根的 `发布清单.md` |
| `--help` | 帮助 |

**依赖**：Node ≥ 18、系统 `zip` / `unzip`（macOS、Linux 自带）。**不引入任何 npm 依赖**，`pnpm install` 都不用跑。

打包时工具会自动做这几件事：复制内嵌技能（自动剔除被内嵌技能自己的检测报告）→ 从 `avatars/` 注入图标 → 校验图标规格与 zip 体积 → 确认 zip 第一层是包目录本身 → 检查发布前检测报告（缺 / 未通过 / 版本不符 / 比源码旧都会告警）→ 调 `scripts/selfcheck.py` 做深度自检 → **排除包内的检测报告** → 通过后才写入 `dist/` → 清理同包旧产物 → 清理 `dist/` 里遗留的旧位置清单 → 重写仓库根 `发布清单.md` 的自动区。**源目录始终不被改动。**

---

## 内嵌技能：构建时才复制，仓库内零副本

专家 / 专家团包要用某个技能时，**包里不放副本**，只在包根放一个描述文件 `skills.json`：

```json
// packages/joy2know-mr/skills.json
{ "skills": ["joy2know-clarify"] }
```

构建时工具会：

1. 从 `packages/joy2know-clarify/` 复制**真实技能**到暂存目录的 `skills/joy2know-clarify/`；
2. 按 `skills.json` **重写包内 `plugin.json` 的 `skills` 数组** —— 两处不一致会告警并纠回；
3. 把 `skills.json` 本身排除在 zip 之外；
4. 若源包里还残留 `skills/` 副本，直接弃用并按描述文件重建。

当前 9 处引用（去重后 8 个技能）：

| 包 | 内嵌技能 |
|---|---|
| `joy2know-mr` | `joy2know-clarify` |
| `joy2know-code-scholar` | `joy2know-codebase` |
| `joy2know-cine-team` | `joy2know-storyboard`、`joy2know-character`、`joy2know-musician` |
| `joy2know-polish-team` | `joy2know-clarify`、`joy2know-humanize`、`joy2know-deck`、`joy2know-dashboard` |

> 同一个技能可以被多个包内嵌（如 `joy2know-clarify` 同时在 `mr` 与 `polish-team` 里）—— 构建时各自从唯一真源复制，不产生副本。

**要改规则，只改 `packages/<技能名>/` 那一份。** 不用再手工 `cp`，也不会出现「两份文本漂成两套口径」。

---

## 图标统一管理

图标**只放 `avatars/`，源包里不放**。三类图标，按名字对应：

| 归属 | 放法 | 构建时去向 |
|---|---|---|
| **专家 / 专家团自身** | `avatars/<包名>.png` | 写到该包 `plugin.json` 里 `avatar` 字段指定的路径（如 `avatars/team.png`） |
| **专家团成员** | `avatars/<包名>/<文件名>.png` | 按**文件名**逐个写入包的 `avatars/`，文件名须与 `members[].avatar` 逐字对应 |
| **技能** | `avatars/<技能名>.png` | ①独立技能包**不注入**（官方技能包没有图标槽位，该图用于后台发布技能时上传）②被内嵌进专家包时复制为 `skills/<技能名>/icon.png` |

**规格：512×512、PNG、单张 ≤500KB。** 尺寸或格式不符会告警，体积超限直接报错。缺图标时该包仍会打包，但会明确告警。

当前需要 30 张：

```
avatars/
├── joy2know-mr.png                       专家自身 · 晓得·表达条理与可视化专家
├── joy2know-code-scholar.png             专家自身 · 晓得·代码考古学家
├── joy2know-cine-team.png                专家团自身 · 晓得·AI 视频生产团队
├── joy2know-cine-team/                   专家团成员 7 张
│   ├── cine-lead.png                     雷导（主理人）
│   ├── cine-narrative.png                诺拉
│   ├── cine-shot.png                     尚恩
│   ├── cine-prompt.png                   珀西
│   ├── cine-consistency.png              柯拉
│   ├── cine-post.png                     奥托
│   └── cine-music.png                    小音
├── joy2know-polish-team.png              专家团自身 · 晓得·文案打磨团
├── joy2know-polish-team/                 专家团成员 5 张
│   ├── polish-lead.png                   闻山（主理人·总编审）
│   ├── polish-clarity.png                林知（条理编辑）
│   ├── polish-distill.png                简宁（提炼编辑）
│   ├── polish-visual.png                 方寸（数据可视化编辑）
│   └── polish-tone.png                   苏白（语言编辑）
└── joy2know-<14 个技能名>.png             14 张技能图标
```

**文件名必须与 `plugin.json` 里 `members[].avatar` 逐字对应**，否则构建时找不到，只会告警不会报错 —— 上传时才发现头像空了。

> ✅ **状态（2026-09-17 晚）：30 张全部已是真图，规格 30/30 合规**（512×512、单张 ≤500KB），占位图残留 0 张。
> 本表**不逐行标「已就位」** —— 图标会随迭代变动，写死在文档里必然过期。**实时状态一律以 `pnpm run list` 的输出为准**（`✓` = 在，`◐` = 技能图标缺，`缺` = 自身图标缺）。
> 技能图标不进 zip（官方技能包没有图标槽位），它是后台发布技能时上传用的；`joy2know-polish-team` 这类专家团缺自身图标时**仍会打包成功，但上传会失败**。文件按上表放好、跑一次 `pnpm build` 即生效。

### 缺图了用脚本补，别手画

```bash
python3 scripts/placeholders.py --check   # 缺哪些 · 哪些还是占位图 · 哪些已换成真图
python3 scripts/placeholders.py           # 补齐缺失的 512×512 占位图（绝不覆盖已有文件）
```

占位图是浅灰相框 + 太阳 + 山形，**底部印着「谁 · 待替换」**（`polish-lead.png` 上写着「闻山 / 主理人·总编审 · 待替换」）——
文件名只说得出 `polish-lead`，图上那句才说得清该画谁。有真图了直接覆盖同名文件即可。

脚本每次生成都会把 sha1 记进 `avatars/.placeholders.json`，所以 `--check` 能准确报出
「还剩几张没换」—— 替换是覆盖同名文件，光看文件在不在没用，得比内容。详见 `avatars/README.md`。

---

## 资产总表

### 技能（14）

| 包名 | 展示名 | 版本 | 作用 | zip | 状态 |
|---|---|---|---|---|---|
| `joy2know-clarify` | 晓得·讲明白 | 1.2.0 | 让任何回答更有条理更好懂：结论先行、分层展开、该画图时主动配图，守住不编造的底线 | 14.3 KB | **已提交** ·【本地升级】 |
| `joy2know-design-tokens` | 晓得·设计解剖 | 1.2.0 | 把设计参考解剖成可复用的 Design Token 文件，再按 token 生成风格一致的页面代码 | 17.3 KB | 待上传 |
| `joy2know-storyboard` | 晓得·分镜工 | 1.2.0 | 把白模视频切成镜头、抽首帧，按目标模型语法产出分镜提示词包 | 13.5 KB | 待上传 |
| `joy2know-character` | 晓得·角色档案 | 1.2.0 | 把角色设定存成结构化角色卡，用锚点短语锁定跨图一致性，解决「同一个人每张图都不一样」 | 9.6 KB | 待上传 |
| `joy2know-asset-ledger` | 晓得·素材台账 | 1.2.0 | 给生成素材建可检索台账：提示词能从 ComfyUI / Stable Diffusion 生成的图里直接挖出来（含压缩块），按创作意图检索而非文件属性，推断「同一批」，可与角色档案 / 分镜工联动，并出单文件 HTML 看板 | 36.2 KB | **已提交**（类目被打回）·【本地升级】 |
| `joy2know-codebase` | 晓得·代码考古 | 1.2.0 | 给陌生仓库半小时产出架构说明、入口链路、数据流和改造风险点 | 12.7 KB | 待上传 |
| `joy2know-logdetective` | 晓得·日志侦探 | 1.2.0 | 把海量日志聚成错误模式、时间线和根因假设，而不是又一份海量文本 | 13.4 KB | 待上传 |
| `joy2know-errorfix` | 晓得·报错翻译官 | 1.2.0 | 把报错和堆栈翻译成人话，并给出可直接复制的修复步骤 | 8.5 KB | 待上传 |
| `joy2know-cmdforge` | 晓得·命令制造局 | 1.2.0 | 把自然语言需求变成可用的正则、命令行或 SQL，附逐段解释、测试用例与危险等级 | 9.5 KB | 待上传 |
| `joy2know-dashboard` | 晓得·一页看懂 | 1.2.0 | 把 Excel 表格数据变成一个能直接发的可交互单文件 HTML 看板 | 8.7 KB | 待上传 |
| `joy2know-deck` | 晓得·讲得清 | 1.2.0 | 把长报告提炼成 15 页能讲的 PPT 结构，每页附口播稿 | 7.6 KB | 待上传 |
| `joy2know-musician` | 晓得·音乐人 | 1.2.0 | 用 Suno 高阶语法产出可直接粘贴的歌曲包，默认治愈风格，支持指定风格与方言并做防跑偏 | 20.4 KB | 待上传 |
| `joy2know-conversation-digest` | 晓得·对话归纳 | 1.2.0 | 只归纳 WorkBuddy 里的对话，还原成「我问了什么 / 怎么答的 / 产出了哪些文件」的学习流水 | 20.4 KB | 待上传 |
| `joy2know-humanize` | 晓得·凡人腔调 | 1.1.0 | 只改中文论述文里规则清单明确命中的机器腔调，其余逐字保留；带规则表自检 + 改写门禁四道检查 | 52.3 KB | 待上传 |

> **发布 zip 的文件名是 `dist/<包名>-v<版本>.zip`**（带版本号，便于区分是哪一版）；每次构建后同包的旧版本 zip 自动清理。
> 表里的体积为**上次构建**的实测值，逐包的最新体积与平台侧版本以**仓库根 `发布清单.md`** 为准（那份是构建时按实际状态生成的）。
> 图标不在此表逐行标注 —— 规格与状态统一见「图标统一管理」一节，缺图不影响打包，但上传前必须补齐。

### 专家与专家团（4）

| 包名 | 展示名（`profession`） | 类型 | 版本 | 内嵌技能（版本） | 成员 | zip | 状态 |
|---|---|---|---|---|---|---|---|
| `joy2know-mr` | 晓得·表达条理与可视化专家 | 专家 | 1.2.0 | `joy2know-clarify` 1.2.0 | — | 740.1 KB | **已提交** ·【本地升级】 |
| `joy2know-code-scholar` | 晓得·代码考古学家 | 专家 | 1.2.0 | `joy2know-codebase` 1.2.0 | — | 737.7 KB | 待上传 |
| `joy2know-cine-team` | 晓得·AI 视频生产团队 | 专家团 | 1.2.0 | `joy2know-storyboard` 1.2.0、`joy2know-character` 1.2.0、`joy2know-musician` 1.2.0 | 7 角色 | 4259.0 KB | 待上传（已补 `settings.json`） |
| `joy2know-polish-team` | 晓得·文案打磨团 | 专家团 | 1.2.0 | `joy2know-clarify` 1.2.0、`joy2know-humanize` 1.1.0、`joy2know-deck` 1.2.0、`joy2know-dashboard` 1.2.0 | 5 角色（1 主理 + 4 成员） | 3920.5 KB | 待上传（已补 `settings.json`） |

> 两个团队的分工边界：`cine-team` 管**从零产出**（一个创意 → 能开拍的方案）；`polish-team` 管**已有材料的收尾**（稿子/数据 → 能交出去的版本）。前者是生产端，后者是加工端。

> **卡片命名规则（2026-09-16 起）**：卡片**主标题取 `profession`、副标题取 `displayName`** —— 与官方文档「displayName 是市场展示名称」的说法相反，以客户端实际渲染为准（截图实证）。三个专家/专家团统一：`profession` = `晓得·<职能>`（英文 `joy2know <Function>`），`displayName` = `晓得乐`（英文 `joy2know`）。
> **技能侧同样统一**：中文 `display_name` = `晓得·<名字>`，英文 `display_name_en` = `joy2know <Name>`（2026-09-16 由 `Xiaode <Name>` 改，13 个技能 + 2 处引用文档里的旧包名 `xiaode-clarify` 一并修正；2026-09-17 新增 `joy2know-humanize`，编号至 14）。

> 体积口径：技能包上限 3 MB、专家包 20 MB、单张图标 500KB@512×512。**图标换成真图后，专家包体积由图标主导** —— 最大的 `joy2know-cine-team` 是 4259.0 KB（其中约 4 MB 是 8 张图标），离 20 MB 上限还有约 4.7 倍余量；同理 `polish-team` 3920.5 KB。
> **技能包不进图标，所以体积仍是几十 KB** —— 技能图标只在后台发布技能时上传，不占 zip 配额。

### 技能发布类目（平台按内容判定，须照选）

技能包内**没有**类目字段 —— 类目是发布时在**后台下拉框里选的**，选错会被打回：
「所选类目与应用类目不符，请重新选择相符类目，至少应包含 ……」。
其中「应用类目」是平台**按技能内容判定**出来的，「所选类目」是发布人手选的。**看平台给的提示照选，不要凭技能名猜。**

| 技能 | 平台判定的类目 | 依据 |
|---|---|---|
| `joy2know-asset-ledger` | **工具 > 图片处理** | 2026-09-23 首次提交被打回，理由原文：「至少应包含工具 > 图片处理类目」 |

- 打回的措辞是「**至少应包含**」，说明该类目选择项**可能允许多选** —— 命中项之外可再补一个近义类目，别只勾一个。
- **类目选错只影响审核，与包本身无关** —— 改类目重新提交即可，不因为类目问题抬版本号。
- 其余技能首次提交时，同样**先看平台给出的判定提示再选**。

> **逐包建议类目已收进 `release.config.json`**（唯一真源），并自动汇总进 `发布清单.md`（仓库根）的自动区。
> 表里只有 `asset-ledger` 那条带 `★已实证`（来自平台驳回原文），其余是**按能力方向给的参考**，
> 发布时以平台给出的判定提示为准。判定结果回来后，改 `release.config.json` 并把 `categoryConfirmed` 置真。

---

## 更新日志

> **自 2026-09-17 起记录。** 此前的变更没有留存，不再追补。
>
> **版本口径**：内容有实质改动 → patch（1.0.0 → 1.0.1）；新增能力、规则或角色 → minor（1.0.0 → 1.1.0）；字段或包结构改动 → major。
> **【本地升级】**标记 = 版本号已在本仓库抬高，但该包**尚未上传平台**，平台侧仍是旧版。
> 状态口径：**已提交** = 已上传平台；**待上传** = 本地就绪、平台没有。两者可叠加【本地升级】—— 如 `joy2know-clarify` 与 `joy2know-mr`：平台上是 1.0.0，本地已是 1.1.0，**要重传一版**。

### 2026-09-17

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `joy2know-humanize` | 新增 | 技能「晓得·凡人腔调」：去中文论述文的机器腔调。13 条规则 + 12 条不可改清单，每条规则自带正反例；四个零依赖脚本（`scan.py` 定位/triage/audit/render、`guard.py` 四道门禁、`selftest.py` 规则表自检、`fix.py` 机械替换）；规则文档由规则表生成，改一处不会漂两处。自检 35 条正例全中 / 42 条反例全不中；门禁经违规样本反向验证 | 1.0.0 |
| `joy2know-polish-team` | 新增 | 专家团「晓得·文案打磨团」：1 主理人 + 4 编辑（条理 / 提炼 / 数据可视化 / 语言），内嵌 `clarify`+`humanize`+`deck`+`dashboard`。**先体检再分棒**，不是固定流水线；**语言编辑强制最后一棒**（结构编辑会重新引入腔调）。zip 107.7 KB，自检 19 项通过；4 条「成员 name ≠ agentName」为专家团已知误报。**尚缺 6 张头像**（团自身 1 + 成员 5），补齐前上传会失败 | 1.0.0 |
| `README.md` | 结构 | 新增本「更新日志」一节；资产总表补版本号列；专家表补内嵌技能版本与成员列；内嵌技能引用表补 `polish-team` 行；图标清单去掉写死的「已就位」状态，改为以 `pnpm run list` 为准 | — |
| `README.md` | 约定 | 维护纪律新增四条：每新增技能必做「归置判断」、每次收工输出全量清单、改内容就动版本号、图标状态不写死在文档里 | — |

### 2026-09-17 · 下午（图标补齐 + 规格工具）

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `scripts/placeholders.py` | 新增 | 图标占位图生成器。一次枚举仓库全部 **30 张**图标需求（14 技能 + 4 专家/团自身 + 12 成员），补齐缺失的 512×512 占位图；**只补不覆盖** —— 已经换好的真图不会被碰。占位图底部印「谁 · 待替换」（`polish-lead.png` 上是「闻山 / 主理人·总编审 · 待替换」），并记 sha1 台账进 `avatars/.placeholders.json`，`--check` 能准确报出「缺几张 / 还剩几张是占位 / 已换成真图几张」 | — |
| `scripts/shrink-icons.py` | 新增 | 图标规格体检。一次扫完 `avatars/` 报出全部不合规项并给出缩后体积；默认只写预览副本到 `avatars/.shrunk/`，加 `--apply` 才替换（原图先备份进 `avatars/.originals/`，该目录被构建忽略） | — |
| `scripts/build.mjs` | 修复 | `--list` 把「成员目录存在」误判成自身图标已就位 —— `joy2know-cine-team` 明明缺 `avatars/joy2know-cine-team.png`，却显示 `✓ +7 成员`，会让人以为不用补。改为成员数只作附加信息：`缺 +7 成员` / `✓ +7 成员` | — |
| `avatars/` | 补齐 | 补上 **16 张**占位图：技能图标 7（`asset-ledger` `character` `clarify` `cmdforge` `dashboard` `deck` `storyboard`）＋ 专家/团自身 4（`cine-team` `code-scholar` `mr` `polish-team`）＋ polish-team 成员 5。**全程未覆盖任何已有文件** | — |
| `README.md`、`avatars/README.md` | 约定 | 新增「缺图了用脚本补，别手画」一节；图标台账与缩图用法写进两处 README；去掉写死的「【待补】」标记 | — |

> **同日下午查明的一个根因**：`avatars/` 下 13 张不合规的图标**全部是 2048×2048** —— 不是个别疏忽，是生图工具的输出规格。2048 的插画轻易 3–4MB，**尺寸超了体积必然超**。但**缩到 512 就够，不用降画质**：实测 2048×2048 / 3.8MB → 512×512 / **334KB 全彩 PNG**，连 256 色都不用降。
> 另：**一个技能图标超限会卡死一串包** —— 内嵌技能的图标会复制进包里一起受校验，所以 `clarify.png`（3.8MB）直接让 `polish-team` 打包失败（是失败，不是告警）。

### 2026-09-17 · 下午二轮（图标全量清单 + 台账口径）

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `avatars/README.md` | 约定 | 全量清单改成 **30 行表格**（类型 / 文件名 / 功能 / **avatar 道具方向**），替换原来的树形清单 —— 树只说得清「放哪」，说不出「画什么」。已画好的 19 张如实描述道具，11 张占位图给出道具方向；「道具方向」一列可直接当生图提示词用 | — |
| `scripts/placeholders.py` | 修复 | `--check` 的统计口径漏算：只统计台账记过的 sha1，**没入台账的图被静默跳过** → 曾报「占位 10 · 真图 5」，加起来 15 ≠ 总数 30。加体积兜底（≤40KB 判为占位图），修后 **缺 0 · 占位 11 · 真图 19 = 30** | — |

> 本轮查出的遗留项（未改）：`joy2know-codebase.png` 与 `joy2know-code-scholar.png` 是同一张图（sha1 相同，内嵌时会与专家头像撞脸）；`avatars/joy2know-design-tokens.jpg`（旧版）与 `avatars/职业-说书.jpg`（不属于本仓任何包）是残留文件；`conversation-digest` 图标 595KB 超 500KB 上限（单独技能包不校验，一旦被内嵌进专家包会打包失败）。
> **以上三项已在下一轮全部清零**，见下。

### 2026-09-17 · 晚（图标全量落地 + 统一版本 1.1.0）

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| **全部 18 个包** | 版本 | 版本号统一由 `1.0.0` 抬到 **`1.1.0`**（14 技能 + 2 专家 + 2 专家团），共改 21 处源文件：14 个 `SKILL.md` 的 `version:`、4 个 `plugin.json` 的 `"version"`、3 个专家/团包内 `README.md` 的版本行 | 1.1.0 |
| `packages/joy2know-mr/README.md` | 补齐 | 该包 README 原本**没有「版本」段**（另三个专家包都有），补上，四个专家/团包结构对齐 | 1.1.0 |
| `avatars/` | 就位 | **30 张图标全部换成真图**，占位图残留 0 张。规格体检：30/30 合规（512×512、单张 ≤500KB）。**上轮的三个遗留项全部清零** —— `codebase` 与 `code-scholar` 已拆成两张不同图（区分点是放大镜有无）、两张 `.jpg` 残留文件已清除、`conversation-digest` 已回到 500KB 以内 | — |
| `dist/*.zip` | 重建 | 18 个包全部重新打包，深度自检逐包通过。**专家包体积因图标换成真图而倍增**：`mr` 367.5→739.5 KB、`code-scholar` 25.0→736.1 KB、`cine-team` 106.4→4256.4 KB、`polish-team` 107.7→3918.3 KB；技能包不进图标，体积基本不变（几十 KB） | 1.1.0 |
| `README.md` | 结构 | 资产总表 18 行版本号与 9 处体积同步刷新；「体积口径」段重写 —— 原写「最大包是 `mr`（367 KB），离上限还有 50 倍余量」已不成立，现最大包是 `cine-team` 4256 KB（约 4.7 倍余量），并点明**专家包体积由图标主导、技能包不受影响** | — |
| `avatars/README.md` | 状态 | 顶部补「30 张全部已是真图」状态行；`codebase` 去掉「与 code-scholar 是同一张」的过期警告，两行改为如实描述放大镜有无；`polish-tone` 改成实际道具 | — |
| `joy2know-clarify`、`joy2know-mr` | 状态 | 这两个包**平台上是 1.0.0、本地已 1.1.0**，状态列补标【本地升级】—— **需要重传一版**，否则「平台与本地版本号相同、内容不同」，下次分不清该以哪边为准。同时补了状态口径里「已提交 +【本地升级】」这一叠加含义 | 1.1.0 |
| `selfcheck.py`（技能侧，仓库外） | 修复 | 修掉两条长期误报，18 个包从「10 项未通过」变为**全绿**：① 专家团成员 `name` 的判定改为「等于 `agentName` **或**命中 `members[].id`」（实测 `agents[]` 文件名去 `.md` 与 `members[].id` 严格一一对应）；② 主源 vs 内嵌副本的 `diff -r` 加 `-x icon.png`（该文件由构建按设计生成，不是漂移）。**两处都做了反向验证**：造错样本仍被抓住 —— 主理人名不符、成员名不在 `members[].id` 中、篡改主源内容，三类全部红灯 | — |

> 本轮**没有修改任何技能内容或专家人设**，纯版本号抬升 + 图标落地 —— 对应「字段或包结构改动」以外的口径说明：这是一次**基线对齐**，把全部包统一到同一条版本线上，便于后续做「平台上是哪一版」的比对。
> `joy2know-humanize` 的**规则表版本**（`scripts/rules.py` 的 `VERSION`、及其生成的 `references/rules.md`）**保持 1.0.0 未动** —— 它是规则集自身的版本，与技能包版本是两个独立概念，本轮规则内容零改动。

### 2026-09-17 · 深夜（专家团 `settings.json` 补齐 · 修复平台解析失败）

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `joy2know-cine-team`、`joy2know-polish-team` | 修复 | **补齐根目录 `settings.json`**（内容与 `setting.json` 相同，各 36 / 38 字节）。上传平台报 `解析失败：settings.json 不存在或无法读取（Team 型专家必须在 plugin root 下提供 settings.json）` —— 此前只放了无 s 的 `setting.json` | 1.1.0 |
| 两个团包内 `README.md` | 修正 | 目录结构补上 `settings.json` 一行并注明两者并存的原因；顺带修正 `cine-team` 的计数错误 —— 原写「1 主理人 + 5 成员」（实为 6）、「团队头像 + 6 角色头像」（实为 7） | — |
| `selfcheck.py`（技能侧，仓库外） | 新增门禁 | 新增专家团根文件校验：`settings.json` 与 `setting.json` **都必须存在、必须是合法 JSON、`agent` 必须等于主理人、两份内容必须一致**。此前门禁完全没覆盖这个文件，所以平台报的错在本地一路绿灯放过了。**已做反向验证**：删掉 `settings.json`、把 `agent` 改成错名，两种情况都被红灯抓住 | — |
| `dist/*.zip` | 重建 | 18 个包全部重新打包，两个团包自检 41 / 40 项、**0 项未通过** | 1.1.0 |

> **根因：官方自己两套说法打架。** 开放平台文档的目录树写的是 `settings.json`，而官方可下载模板 `trading-team.zip` 里用的是 `setting.json`。本仓库当初按模板实物取名 —— 结论「官方文档正文误写成 `settings.json`」并写进了维护技能，**实为反了：平台校验器认的是 `settings.json`**。现在的做法是两个名字都放（各几十字节），谁认哪个都不落空。
> 两个包**未抬版本号**：平台侧从未成功解析过 1.1.0，本次是让 1.1.0 能通过解析，不构成新版本。

### 2026-09-18 · 凌晨（专家团 `categoryId` 修正 · 修复平台分类校验）

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `joy2know-cine-team`、`joy2know-polish-team` | 修复 | **`categoryId` 由 `00-ExpertTeam` 改为 `06-ContentCreative`（内容创作）**。上传平台报 `categoryId "00-ExpertTeam" 不在合法分类列表中，请参考开发规范第八节` | 1.1.0 |
| `selfcheck.py`（技能侧，仓库外） | 新增门禁 | 新增 `categoryId` 白名单校验：**必须落在官方「行业分类」15 类内**，缺失或越界均判错，并内建 `VALID_CATEGORY_IDS` 常量。**已做反向验证**：原错值 `00-ExpertTeam`、编造值 `99-Whatever`、删字段三种情况全部红灯，合法值绿灯 | — |
| `dist/*.zip` | 重建 | 18 个包全部重新打包，四个专家/团包自检 42 / 40 / 23 / 23 项、**0 项未通过**。已核 zip 顶层只含包目录一层、无 `.DS_Store` 混入 | 1.1.0 |

> **根因：`00-ExpertTeam` 这个分类根本不存在。** 平台只有一份分类清单（官方文档「行业分类」节的 **15 类 `01`~`15`**，含 `15-Education`），**专家与专家团共用**，专家团**没有专属分类**。本仓库当初误以为模板里的值是专家团专用的 —— 实际上官方 `trading-team` 模板用的是 **`08-FinanceInvestment`**，是把模板原文读错了；而且**专家团那一页文档压根没有分类表**，这大概也是当初没核对上的原因。
> 两个包**未抬版本号**：同 09-17 深夜的道理 —— 平台侧从未成功解析过 1.1.0，这是让 1.1.0 能过校验，不是新版本。

### 2026-09-23（素材台账 1.2.0 · 被打回后重做）

> 起因：`joy2know-asset-ledger` 首次提交被平台打回，理由是**类目选错**（见上文「技能发布类目」）。
> 但顺手体检这个技能时发现，它「过于简单」不是行数问题 —— **SKILL.md 写了三条硬承诺，脚本一条没兑现、两条做错了**。
> 于是这一版把承诺补成事实。

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `joy2know-asset-ledger` | 修复 | 两处「文档写了、脚本没做」：① `--format csv` 时增量判定键（`_size`/`_mtime`）被列名裁掉，**二次扫描同一目录会重复入库**（实测 2→4 条）；② 不传 `--scrap` 时**废片照常返回**，与「检索默认排除废片」相反（实测 2/2 全中）。另加重复记录自动收敛，旧版 CSV 留下的脏台账读一次就干净 | 1.2.0 |
| `joy2know-asset-ledger` | 新增 | 补上缺失的**写入入口** `set`（补字段 / 标废片 / 改可商用 / 追加标签），以及体检 `stats`（废片率、字段完整度、来源分布、隐私提醒）。此前「缺字段由用户补充」只在文档里，用户只能手改台账文件 | 1.2.0 |
| `joy2know-asset-ledger` | 新增 | **从图内元数据直挖提示词**：认 PNG 的 `tEXt` / `zTXt` / `iTXt` 三种文本块，支持 ComfyUI（`prompt` JSON，**含 zlib 压缩块**；靠 `KSampler` 连线判定正负，拿不到连线才退标题启发式并记录依据）、Automatic1111 / Forge（`parameters` 文本，正确处理引号内的逗号）、InvokeAI / Fooocus / NovelAI | 1.2.0 |
| `joy2know-asset-ledger` | 新增 | **批次推断**：按「文件名族 + 生成时间邻近」聚批，一律标 `[推断]`，依据分 `filename+time` / `filename-only`。文件属性只作**推断线索**，不作检索滤镜 —— 规则 1 红线补了边界说明，避免「推断批次」与「禁止按时间检索」自相矛盾 | 1.2.0 |
| `joy2know-asset-ledger` | 新增 | `render` 出单文件浅色 HTML 看板（统计条 + 缩略图网格 + 前端筛选 + 提示词预览）；`query` 加 `--grep-prompt` 提示词全文检索、`--batch` 按批次捞、`--min-long-side` 按长边筛 | 1.2.0 |
| `joy2know-asset-ledger` | 新增 | **与同系列联动**：`--characters` 接角色卡 —— 校验 `role` 是否在卡里，并逐条比对锚点短语有没有进提示词（全不中报 `anchor-miss`、部分中报 `anchor-partial`），相当于**生成后的一致性校验**；`--storyboard` 接分镜产出 —— `shot_*` 素材自动带上对应镜头的提示词与实测时长。两条都**只读不改**：不代填 `role`（那是凭文件名推断，规则 2 禁止）、不改写分镜提示词 | 1.2.0 |
| `joy2know-asset-ledger` | 新增 | `references/metadata-sources.md` —— 各生成工具把提示词藏在图的哪里（三种文本块的区别、ComfyUI 取哪个 JSON、A1111 的两个解析陷阱、**已知不支持项**与手工验证方法）。补上「references 为空」 | 1.2.0 |
| `scripts/build.mjs` | 修复 | 打包时排除 `__pycache__/` 与 `*.pyc` —— 只要在包内跑过 Python 脚本就会生成，进包会被深度自检判为「含临时文件」。此后无需人工清理 | — |
| `README.md` | 约定 | 资产总表下新增「技能发布类目」小节 | — |

> **自检**：本包深度自检 **8 项通过 / 0 项未通过**；zip 36.2 KB（上限 3 MB）。
> **实测**：14 组端到端用例全通过 —— jsonl 与 csv 双格式增量、废片三态过滤（默认 / `--scrap true` / `--all`）、四种工具元数据解析（含 zTXt 压缩块与引号内逗号）、中英文全文检索、批次推断、角色卡联动（YAML 与 MD 两种格式、未知角色、锚点全未命中 / 部分命中）、分镜联动（首帧图与片段接上提示词与时长）、`--shot` 镜头检索、旧台账兼容、重复记录收敛、空台账降级、HTML 看板缩略图可达。

### 2026-09-23 · 二轮（发布流程工程化 + 知识固化进仓库）

> 起于 9 条具体要求：产物带版本号、发布清单改名并自动化、逐包列最后版本、加发布历史、加建议发布类目、
> 把发布流程写死进 `AGENT.md`、建立发布前检测（含**防假测试**）、检测结果落成可复用的文档、
> 检测文档不进 zip。外加一条：**把个人技能里的知识固化进本仓库**，让它脱离本机技能目录也能独立跑。

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| `AGENT.md` | 新增 | 仓库工作手册：**发布流程七步**（改动 → 升版本 → **发布前检测** → 写报告 → 构建 → 提交 → 发布记账），**发布前检测四道门**（①功能测试真跑 ②完整性对照 ③合理性 ④**测试真实性 / 防放水**），检测报告规范与复用规则，命令速查，五段式提交约定，红线清单。**硬门禁：四道门没过不许打包** | — |
| `docs/平台资产规范.md` | 新增 | 完整规范手册（搬自个人技能 `WorkBuddy资产包生产`）：三种上传对象的结构与全字段、SKILL.md 9 必填（含官方字段表漏列的 3 个）、plugin.json 16 字段与平台硬约束、`categoryId` 官方 15 类清单、体积上限、头像三类去向与规格陷阱、**官方模板 ≠ 规范**的两次踩坑、上传前必须由人做的四件事、发布类目、许可与署名、归置判断、平台报错速查 | — |
| `scripts/selfcheck.py` | 新增 | **自检脚本搬进仓库**（原在 `~/.workbuddy/skills/WorkBuddy资产包生产/`）。这是本仓库此前**唯一的硬外部依赖** —— 换机器就没法做字段自检。搬入时改了两处：缺 zip 时记提示而非失败；**内嵌副本漂移比对排除 `<技能名>.md`**（被内嵌技能的检测报告，设计上不进包） | — |
| `scripts/make-avatar.py` | 新增 | 零依赖、零积分画头像的脚本一并搬入（改顶部常量换配色即可重跑） | — |
| `scripts/build.mjs` | 新增 | ①产物改为 **`dist/<包名>-v<版本>.zip`**（带版本号），并**自动清理同包旧版本 zip**（`--keep-old-zips` 可关）；②**自动重写 `dist/发布清单.md` 的前四节**，`<!-- AUTO:END -->` 之后的人工段落原样保留；③**排除包内检测报告**（含被内嵌技能那一份）不进 zip；④**发布前检测门禁**：报告缺失 / 状态为「未检测·未通过」/ 版本不符 / 报告比源码旧 → 告警，`--strict` 直接失败；⑤`--list` 与打包汇总表都加上版本号与产物文件名 | — |
| `scripts/init-checkdocs.mjs` | 新增 | 检测报告骨架生成器：**只生成骨架不做结论**（状态一律 `⛔ 未检测`），按包类型预列待验证项（脚本 / 参考文档 / 内嵌技能 / 团队 settings.json / 反向验证），**不覆盖已有报告** | — |
| `release.config.json` | 新增 | 发布参考**单一真源**：逐包建议发布类目（含 `categoryConfirmed` 标记平台实证）+ 平台侧最后版本与备注。清单里的类目与状态列由它驱动，避免人在两处维护 | — |
| `packages/*/<包名>.md` | 新增 | **18 份发布前检测报告**，与包目录同名、放包内。`joy2know-asset-ledger` 那份是**基于本轮真实实测**写成的（14 组用例 + 4 组反向验证 + 7 项未覆盖项，状态 `⚠️ 有条件通过`）；其余 17 份为骨架（`⛔ 未检测`，预列待验证项），**不编造结论** | — |
| `dist/发布清单-1.1.0.md` → `发布清单.md` | 改名 + 迁到仓库根 | 去掉文件名里的版本号；改为「自动区 + 人工区」结构；逐包列**本地版本 / 发布 zip 文件名 / 体积 / 建议发布类目 / 平台侧最后版本 / 状态**；末尾新增**「六、发布历史」** | — |
| `发布清单.md`（仓库根） | 新增 | 发布历史表（已知事实如实记录：`clarify` 与 `mr` 平台侧 1.0.0、`asset-ledger` 1.1.0 因类目被打回；**上传日期当时没记的写「日期未记录」，不编日期**） | — |
| `package.json` | 新增 | `pnpm run checkdocs`、`pnpm run selfcheck` 两个入口 | — |
| `README.md` | 结构 | 目录结构树补 `AGENT.md` / `docs/` / `release.config.json` / 检测报告 / 版本化产物；快速开始补新选项与新命令；新增「发布前检测」与「发布清单」两节；维护纪律补 5 条（12–16）；「相关技能」改为外部依赖已固化的对照表；顺带修正归档目录名（`第一阶段学习流水/` → `private/`） | — |

> **本轮修掉的三个自造缺陷（都是写这一轮功能时引入的，均已反向验证）**：
> ① 排除检测报告时按**文件名**匹配，把专家包内与包同名的 agent 文件（`agents/joy2know-code-scholar.md`）误删 —— 改为**只排除包根那一个**，并确认 agent 文件回到 zip；
> ② 被内嵌技能的检测报告漏排除，跟着进了专家包（`skills/<技能名>/<技能名>.md`）—— 构建时一并剔除，并在自检的漂移比对里排除它（**否则会反过来误报漂移**）；
> ③ `joy2know-asset-ledger` 的核心规则小节顺序为 1→2→3→4→5→**7→6** —— 这是本轮体检（门 3 合理性）查出来的，已调整。
>
> **反向验证**：给自检的漂移比对加了排除项之后，造了「只差 icon.png 与检测报告」（应判一致）、
> 「篡改副本 `repo_scan.py`」、「篡改副本 `SKILL.md`」三种样本 —— 后两种仍被抓，**排除没有变成橡皮章**。
>
> **现状**：18 个包全部构建通过，深度自检 **0 项未通过**；`joy2know-asset-ledger` 的报告是唯一
> 非「未检测」的一份，其余 17 个包在每次构建时都会被点名告警 —— **这正是门禁该有的样子**。

### 2026-09-23 · 三轮（18 包全量检测 → 缺陷修复轮）

> 起于一条指令：「对 18 个包做发布前检测，一个做完再触发下一个，自己 loop 到完成」；
> 检测完成后追加一条：「除了『未文档化参数』外其它都改掉」，并明确口径：
> **凡进包内容的改动都要抬版本，只有不进包的文档（检测报告 / 发布清单 / 仓库级 docs）不抬。**

**一、检测：18/18 成文**。全部 `⚠️ 有条件通过`，七节齐全、头部为构建门禁要求的**列表格式**、未覆盖项非空。
6 个含脚本的包由后台子代理并行做，其余 12 个在主线程做。构建 **0 告警** —— 门禁认可全部报告且判定为最新。

**二、先立规矩，再动手**（散着改 N 遍同一个错，是这个仓库之前最大的浪费）：

| 对象 | 类型 | 变更 |
|---|---|---|
| `docs/平台资产规范.md` | 新增第十二节 | **六条系列硬规则 R1–R6**：R1 降级必须留痕（跳过/失败/丢弃/猜测都要有可见痕迹，**推断值不得与实测值同形输出**）；R2 实现有的能力文档必须写；R3 同一规则只允许一种表述；R4 references 的**必做类**章节必须被 §七 路由点名；R5 派生数值必须自洽可推；R6 同类缺陷要批量扫而不是逐个撞 |
| `AGENT.md` | 铁律 8 → 9 条 | 加第 9 条：**改完必须过 R1–R6**，并把「批量扫同类缺陷」写进发布流程第 ③ 步 |
| `scripts/series_audit.py` | 新增 | 跨包审计脚本入仓（原先只是 `/tmp` 里的临时物）。专查**同一缺陷在几个包里复现**：【A】§七 漏「必做类」章节点名；【B】脚本参数未进文档。**判据收紧过两轮**——初版把概览章（`一、是什么`）与 `--verbose`/`--json` 类通用调试开关也算缺陷，报得越宽越没用 |
| `scripts/selfcheck.py` | 修 | 专家校验里补 **`members[]` 每项字段集合必须恰为 `{id,name,profession,avatar,role}`**（多/少即 `[XX]`）—— 这是 polish-team 反向验证 S4「加字段仍全绿」抓出的盲区 |

**三、修复：17 包抬到 1.2.0，1 包留 1.1.0**。每个包都做了**改前复现 → 改 → 改后复验 → 反向验证**（同一套用例对 git 原始版本跑，确认能抓到缺陷，不是橡皮章）。

| 包 | 修的缺陷（分级） | 双向验收 |
|---|---|---|
| `logdetective` | **中** 时间过滤在同一天边界失效（`--until` 让上界日整天报废）；按「类首现」而非逐条过滤；过滤后上下文仍取自全文 | 12/12 vs **2/12** |
| `codebase` | **中** `pyproject.toml` 把 `[project]` 表键当依赖 + 同生态覆盖赋值**静默丢依赖**；`Gemfile` 落朴素分支 | 改前/改后对照 + node/rust/php/go 回归逐字未变 |
| `storyboard` | **中** 推断时长与实测值**同形输出**；片段抽取失败**完全静默** | 18/18 vs **9/18** |
| `musician` | **中** 非法 UTF-8 抛**裸堆栈**（`except` 只抓 `OSError`）；繁体粤语污染**静默漏报**；降级分支**不可达** | 15/15 vs **7/15** |
| `design-tokens` | **轻** `--css-only` 是**死旗**（声明了、函数体内零引用）；非 UTF-8 **静默替换解码** | 19/19 vs **11/19** |
| `conversation-digest` | **轻** 坏 `arguments` 的写文件调用**静默丢弃**；SKILL 的数据来源归因与实现不符 | 22/22 vs **11/22** |
| `deck` | **中** 降级路径「60 秒 / 40 字」自相矛盾（差 3 倍）；口播字数上限窄化；自检清单漏规则 6；五模板收尾无页号；§七 漏必做章 | 5/5 vs **0/5** |
| `cmdforge` | **中** 四级定义**不足以判定**两类（`chmod -R /` 该极高危却标谨慎、`> file` 覆盖无据）；规则 4 缺优先级；§七 漏必做章 | 5/5（**20 条模式逐条回测**）vs **0/5** |
| `dashboard` | **轻** 规则 1 允许的「树图」不在自称完整的权威表里；涨跌配色无判据；§七 漏必做章 | 3/3 vs **0/3** |
| `clarify` | **轻** 决策表第 9/10 行「别画」无落地判据；「完整对照」失实；§四 引用错列号 | 4/4 vs **0/4** |
| `character` | **轻** 「换造型」落盘形态两处不一致；表注与列结构不符；§七 漏必做章 | 4/4 vs **1/4** |
| `errorfix` | **轻** 路由 D 输出形态两处冲突 | 3/3 vs **1/3** |
| `cine-team` | **中** README 未跟音乐人角色（四处漏项）；`displayDescription.en` 角色数与实际不符 | 9/9 vs **3/9** |
| `polish-team` | **轻** `description` 502 字符（兄弟包 175–197）；校验器盲区（已在工具侧修） | 5/5 vs **3/5** |
| `mr` | **轻** README H1 不合系列写法；目录树未标构建注入 | 7/7 vs **5/7** |
| `code-scholar` | **轻** README 目录树未标构建注入 | 5/5 vs **4/5** |
| `humanize` | — **四道门全过、0 缺陷** | **未改内容 → 留 1.1.0** |

**四、跨包系统性发现：「静默」是本批最高频的失效模式**。6 个互不相关的包各自独立出现
（storyboard 片段抽取、logdetective 过滤返回空、codebase 丢依赖、musician 漏报、design-tokens 解码、
conversation-digest 丢弃写文件调用）。→ 这正是立 R1 的原因：**「不崩」不等于「没错」，静默失败一律记缺陷**。

**五、两处口径纠正（都靠实测推翻原判）**：

- **「239–386 字」是技能包口径，不是专家包口径**。用 YAML 正式解析 14 个技能包实测：
  min 239（clarify）/ max 386（errorfix）/ 中位 364 —— 与区间完全吻合。
  而**专家包** `plugin.description`(en) 实测兄弟包为 **176 / 175 / 197**，本系列根本不在 239 之上。
  → 原报告据此把 code-scholar 的 175 判成「偏短」，属**误报**，**已改判、未改数据**；
  并写了口径探针脚本证明「同一个区间误用于专家包会产生 3 条不存在的缺陷」。
  polish-team 的 502 在两套口径下都判偏离 → 属**真缺陷**，压到 201。
- **`displayName` 与角色代号是两个位**。原报告说 `mr` 的 `displayName`=「晓得乐」与自称「晓得先生」不一致。
  实测四个专家/专家团的 `displayName.zh` **全部是「晓得乐」**，而每包另有**自己的角色代号**
  （考古先生 / 晓得先生 / 总导演组 / 打磨团）→ 系**既定设计**，不是缺陷。**命名未动**，
  并把「不许动」写成两条**守卫判据**，防止后来者再照误判去「统一命名」。

**六、三次自己踩的坑（比包的缺陷更值得记）**：

> ① **派子代理时把报告头部误写成引用块格式** —— 构建门禁解析的是**列表格式**（`- **检测状态**：`），
> 结果 4 个子代理照抄、3 个包写成双头部。已逐个更正并复核 18 份头部。
> **派活时必须把格式原文贴进提示词，不能凭记忆写。**
> ② **我自己的审计脚本误报 4 次**：通配 `packages/*/*.md` 把 `README.md` 当报告（同包出现两条）；
> 正则把**调用点** `args.css_only` 算进函数体（误判死旗「有使用」）；节名按「门 2」查而报告写「门2」；
> 未覆盖项用了标题式写法不认。→ 固化为**校验器三件套**：**精确路径取文件 + 容错匹配 + 阳性对照**。
> ③ **两次「修完仍亮红灯」是真信号**：dashboard 把作废的旧配色默认值**逐字留在括注里**、
> character 写「不是左右列对照」（字面仍含「右列」）—— 都立刻被抓。
> **「不要用某个说法」的说明不能把那个说法原样写出来**，否则模型照抄概率不比原来低。已写进 R3。

> **现状**：18 包构建全过、**0 告警**、深度自检 **0 项未通过**、`packages/` 内无 `__pycache__`；
> `series_audit.py` 的【A】段**已清空**（所有 §七 漏章补齐）。
> 未提交 git（按约定推送由人做）；`dist/` 已重建，`发布清单.md` 随构建刷新。

### 2026-09-23 · 四轮（改名 `joy2know-mr` → `joy2know-articulate` 被平台驳回 → 已回退）

> 起于一条指令：「把 mr 改个语义化的名字」。**动机是成立的** —— `mr` 取自「先生」的 Mr.，是**角色意象**，
> 单看包名看不出这个包是干什么的。改名按 8 处联动全做完、构建 0 告警、自检 11 项通过 —— 然后**在平台上被驳回**。

| 对象 | 类型 | 变更 | 版本 |
|---|---|---|---|
| 包名（8 处） | 改名 → **回退** | 目录 · `plugin.json` 的 `name`/`agentName`/`plugin`/`agents[]` · 角色文件名与 frontmatter `name` · 检测报告文件名 · `avatars/<包名>.png` · `.placeholders.json` 台账 key：先全改成 `joy2know-articulate`，**被平台挡回后全部回退**。去、回两次都用 `git mv`，git 历史干净 | 1.2.0（未动） |
| 平台驳回 | **实证** | 驳回原文：「`包内 name "joy2know-articulate" 与当前资产的 name "joy2know-mr" 不一致：name 是资产的唯一标识，不允许修改。请将 plugin.json 中的 name 改回 "joy2know-mr" 后重新打包上传`」。→ **`name` 是平台侧资产唯一标识、不允许修改**；改名的代价不是「多一步」，而是**掐断「更新」链路**（平台按 `name` 认资产，对不上就不让传） | — |
| `docs/平台资产规范.md`、`AGENT.md` | 立规矩 | 新增 **R7**：①改包名要同改 8 处（逐条列了漏改后果）②**已上线的包，`name` 视为冻结**。`AGENT.md` 铁律 9 由「六条硬规则」改「七条」。顺手修掉维护纪律 16 的歧义：原文「改名不影响上传」容易被读成「包名随便改」 | — |

**四条实证结论（比原来那条推测值钱）：**

> ① **`name` 用户看不到。** 卡片上显示的是 `profession`（「晓得·表达条理与可视化专家」），本来就语义清晰 ——
> `name` 纯粹是平台侧的机器主键。**改名的收益只落在仓库内部，代价却是丢掉整条更新链路，不划算。**
> ② **想换标识只有一条路**：放弃这个资产、以新名重新上架（已上线状态、审核结果一起没）。
> 平台是否允许「删除后立刻用新名新建」**未验证**，且对已上线的包没必要验证 —— 结论已经是不做。
> ③ **8 处联动清单仍然有效**，只是适用对象收窄为**从未上线的包**（15 个 `platform: null` 的包改名不受此限）。
> ④ 过程本身是干净的：改名与回退全程**没有 git 提交**（工作区改完直接重建），所以撤销是免费的 ——
> 若当时先提交再改名，回退就得多一轮 revert 提交。**「改标识」这类高风险动作，先验证再提交**这条值得保留。


---

## 第一阶段归档

`private/` 是这批资产的诞生记录，**内容一字未改**。

| 文件 | 性质 |
|---|---|
| `private/第一阶段流水.html` | 四对话复盘流水：我问了什么（结构化改编）→ Buddy 怎么答（精简）→ 产出了什么（产物总表）。含时间轴、计数口径、沉淀结论、未完结事项 |
| `private/产物/*.html`（14 份）+ `private/产物/overview.md` | 四个对话的全部报告原文，逐字节校验一致 |

**15 个资产包已从 `产物/晓得-交付包/` 迁至 `packages/`**（那是它们的活源，需要跟着版本走）；旧 zip 已删除，改为由 `pnpm build` 生成到 `dist/`。头像生成脚本 `make-avatar.py` / `make-avatar-set.py` 已移除，图标改由 `avatars/` 统一管理。

> 四个对话的分工（引用时直接用这个框架）：
> ① `b72bc039` 生态认知与行业机会（企业视角）→ ② `39cc72a4` 选题 → 竞品扫描 → 方向收敛 → 平台机制解剖 → ③ `43567ae6` 模板解剖 + 晓得立项 + 首发上架 → ④ `d760b47f` 系列批量交付。
> 提问计数：原始 43 条 → 去重 35 条 → 实质提问 32 条（①4 / ②16 / ③9 / ④3）。

---

## 发布前检测：打包之前必须过四道门

**⛔ 硬规矩：没过发布前检测，不许打包上传。** 完整流程见 [`AGENT.md`](AGENT.md)，这里只说机制。

每个包在 **`packages/<包名>/<包名>.md`** 有一份检测报告（与包目录同名）。它有两个特点：

- **不进 zip** —— 构建时自动排除（被内嵌技能的那份也一并剔除），它纯属开发期文档
- **进 git** —— 要能看历史，也要让下一次检测先读它

报告头部有四行是**构建脚本用来判门禁的**，格式不能改：

```md
- **对应版本**：1.2.0
- **检测状态**：⚠️ 有条件通过
- **最后检测**：2026-09-23
- **检测方式**：功能测试 + 完整性对照 + 合理性检查 + 反向验证
```

| 状态 | 含义 | 能否打包 |
|---|---|---|
| `✅ 已通过` | 四道门全过，无遗留未覆盖项 | ✅ |
| `⚠️ 有条件通过` | 四道门已过，但存在明确的未覆盖项 | ✅（未覆盖项要写清） |
| `⛔ 未检测` | 还没测，或只有骨架 | ❌ 构建告警；`--strict` 失败 |
| `❌ 未通过` | 有硬缺陷未修完 | ❌ 构建告警；`--strict` 失败 |

四道门：**① 功能测试（真跑）② 完整性对照（文档承诺逐条核实现）③ 合理性（边界 / 降级 / 自洽 / 零依赖）④ 测试真实性（防假测试）**。
第四道门专门防放水，列了七种假测试形态（只跑 happy path、只看没报错、把「文档写了」当「已验证」、自造理想样本、
跳过失败项、不跑回归、造错样本只造会被抓的那种），并要求**反向验证**：改完校验器要故意造错样本，确认仍然抓得住。

**报告是给下一次的自己省时间用的。** 下次检测先读它：已验证且未变的部分可以信任，**只重测有变化的部分 + 报告里
列出的「未覆盖项」**，每次顺手消掉一条。构建会检查报告是否过期（包里有文件比报告新 → 告警）。

```bash
pnpm run checkdocs            # 看全部包的检测状态；缺的自动补骨架（不覆盖已有）
pnpm run checkdocs -- --list  # 只看状态，不写文件
```

> **骨架不等于结论。** 新生成/未检测的包一律标 `⛔ 未检测` —— 结论只能来自真实的四道门检测。

---

## 发布清单：自动生成，人不手改表格

**仓库根 `发布清单.md`**（不在 `dist/` —— 那里是 gitignore 的产物目录，而清单记着发布历史，属于要进 git 的资产）结构是「**自动区 + 人工区**」：

- 前四节（版本一览 / 技能 / 专家 / 专家团 / 产物规则）由 `pnpm build` **按 `packages/` 的实际版本 + `release.config.json` 重写**，数据不会漂；
- `<!-- AUTO:END -->` 之后的「五、发布时要注意」「六、发布历史」**人工维护，构建原样保留**。

所以：

- 要改**建议发布类目 / 平台侧最后版本** → 改 `release.config.json`（唯一真源）；
- 要记**某次发布** → 在「六、发布历史」追加一行（日期 · 包名 · 版本 · 上传的文件名 · 结果）；
- **不要去改自动区的表格** —— 下一次构建就覆盖了。

`★已实证` 标记表示那条建议类目来自平台的判定/驳回原文（目前只有 `joy2know-asset-ledger` 一条）；其余都是**方向参考**，
发布时以下拉框里最接近的一项为准。

> `dist/` 已被 gitignore —— **发布清单本身不进 git**，`git clean` 或删 `dist/` 会连发布历史一起丢掉。
> 如果希望历史随仓库留痕，把清单移到仓库根即可（改 `scripts/build.mjs` 里的 `MANIFEST` 一行）。

---

## 维护纪律

1. **同一份规则只存一份。** 内嵌技能由 `skills.json` 声明、构建时复制，仓库里**没有**副本可改；改规则只改 `packages/<技能名>/`。
2. **图标只在 `avatars/`。** 源包里不放图标，避免两处不一致。
3. **`dist/` 是产物、不入库**（已 gitignore）；`.workbuddy/` 是运行时数据，同样不入库。
4. **提交前跑 `pnpm build`**，确认深度自检通过。**目标状态是「0 项未通过」** —— 2026-09-17 修掉了两条长期误报（专家团成员的 `name` 判定、内嵌副本把 `icon.png` 误判成漂移），18 个包现已全绿。**不再有「可忽略的误报」这一说**：看到红灯就查，不要习惯性划掉。自检脚本自 2026-09-23 起**已搬进本仓库**（`scripts/selfcheck.py` 是权威版本），不再依赖本机技能目录。
5. **上传前由人来做的事**：实名认证 + 人脸核身、点「公开发布」、装回来跑一次验收。
6. **每新增一个技能，必做一次「归置判断」。** 四选一：①保持独立技能 ②并进现有专家 ③新建单专家 ④与别的技能组成专家团。**给出结论和理由后先问再动手**，不要默默建包。
7. **每次收工输出全量清单。** 技能 / 专家 / 专家团逐项列出，**带版本号**；本轮改过版本的标【本地升级】。清单同时体现内嵌技能与成员构成。
8. **改内容就要动版本号。** 版本号跟着包走，不跟着平台走 —— 本地改了但还没传，就要标【本地升级】，否则下次分不清平台上是哪一版。
9. **图标状态不写死在文档里。** 图标变动频繁，任何「已就位 / 待替换」的静态标注都会过期。**唯一权威是 `pnpm run list` 的实时输出**，文档只负责写清「需要哪些文件、什么规格、放哪里」。
10. **技能发布类目看平台判定，不靠猜。** 技能包内没有类目字段，类目是**后台下拉框选的**；被驳回「类目不符」只影响审核，**改类目重提即可，不因此抬版本号**。判定结果回填到「技能发布类目」一节，供下一个技能参考。
11. **文档写了的能力，脚本必须有。** 出过这个坑：SKILL.md 写了「检索默认排除废片」「缺字段由用户补充」，脚本里一条没实现、一条做反了 —— 使用者按文档操作却拿到错误结果。**承诺与实现之间不留缺口，改完必须实测（含反向验证）。**
12. **打包前必须过发布前检测四道门。** 谁打包都一样：AI 代打包也要先做检测，证据就是 `packages/<包名>/<包名>.md` 里**有本轮日期、对应版本等于当前包版本、状态不是「未检测」**。构建会替你把这道关（缺报告 / 未通过 / 版本不符 / 报告比源码旧都会告警，`--strict` 直接失败）。
13. **检测报告只增不改历史。** 每次检测**追加**「检测履历」一行，并更新头部四行；「未覆盖项」**不许空着**（空着 = 没认真写）。下次检测先读报告，只重测有变化的部分与未覆盖项。
14. **报告是开发期文档，不进 zip —— 但进 git。** 构建已自动排除（含被内嵌技能的那一份）；它只在 `packages/` 里活着，别指望从 zip 里看到它。
15. **发布要记账。** 真去平台上传/上线后：把 `release.config.json` 里该包的 `platform` 字段补上，并在 `发布清单.md` 的「六、发布历史」追加一行。**平台侧的版本号不补，清单就会一直显示「待上传」，下次分不清该发哪一版。**
16. **产物带版本号，同包只留最新一版。** `dist/<包名>-v<版本>.zip`；每次构建自动清理该包的旧版本 zip。zip 第一层仍是包目录名，**外层 zip 文件名不参与平台解析**（带不带版本号都一样）。**注意别把这句读成「改名随便改」** —— 平台解析不了的是外层文件名，而 `zip` 第一层目录名与 `plugin.json` 的 `name` **是**平台侧标识，改包名要过 R7（同改 8 处，见 `docs/平台资产规范.md` 第十二节）。
17. **每次沾发布，都要更新 `发布清单.md`（仓库根）。** 表格别手改（构建会覆盖），要改数据去改 `release.config.json`；但**「六、发布历史」必须每次追加一行**，被打回还要在「五、发布时要注意」写清可执行的下一步。**这是 AI 的固定动作，不是等人的动作**；日期不知道就写「日期未记录」，**不许编**。
18. **推送是人做的事，AI 不碰远程。** 不执行 `git push`，也不去核对本地领先远端几个提交 —— AI 的边界是把提交做在本地。

---

## 相关技能

**本仓库现在自给自足，不依赖任何本机技能目录。** 下面这些曾是外部依赖，2026-09-23 已把知识固化进仓库：

| 曾经依赖 | 现在在哪 |
|---|---|
| `~/.workbuddy/skills/WorkBuddy资产包生产/scripts/selfcheck.py` | `scripts/selfcheck.py`（**仓库这份是权威版本**；构建优先用它，本机技能那份只作兜底，可用环境变量 `SELFCHECK=<路径>` 临时替换） |
| 该技能 SKILL.md 里的字段 / 结构 / 体积 / 类目 / 许可口径 | `docs/平台资产规范.md` |
| 该技能里的头像绘制脚本 `make-avatar.py` | `scripts/make-avatar.py` |

换机器、换 harness 时：读 `AGENT.md` 走流程，读 `docs/平台资产规范.md` 查口径，`pnpm build` 打包。
**唯一的运行时外部依赖是 Node ≥ 18 与系统 `zip` / `unzip`。**

---

## 许可协议

本项目采用 **[署名 4.0 国际（CC BY 4.0）](https://creativecommons.org/licenses/by/4.0/)** —— **您可以自由使用，惟须署名。**

| 项 | 内容 |
|---|---|
| 许可协议 | CC BY 4.0（Attribution 4.0 International / 署名 4.0 国际） |
| 覆盖范围 | 本仓库全部内容：14 个技能包、2 个单专家、2 个专家团，含其中的文档、提示词、脚本与图标 |
| 您可自由 | **共享**（以任何媒介或格式复制、发行）· **演绎**（修改、转换、以本作品为基础再创作）· 用于任何目的，**含商业目的** |
| 您须遵守 | **署名（BY）** —— 给出适当的署名、指向本许可协议的链接，并指明是否作出修改；不得以任何方式暗示作者为您或您的使用背书；不得施加法律条款或技术措施，限制他人行使本许可允许的权利 |
| 版权归属 | 晓得乐 / joy2know（Tairraos）—— 署名权不可转让 |
| 全文 | [`LICENSE`](LICENSE) —— 前附中文说明，后附 CC 官方英文法律文本，**一切以英文法律文本为准** |

**署名格式（引用、转载、二次分发时请照此标注）：**

```
原作：晓得 / joy2know（作者：晓得乐 / Tairraos）
来源：https://github.com/Tairraos/joy2know-skills
许可：CC BY 4.0 — https://creativecommons.org/licenses/by/4.0/
```

> 资产包上传到 WorkBuddy 平台后，平台内的展示与使用还受平台自身协议约束；本许可约束的是**本仓库内容本身的**复制、改编与再分发。
