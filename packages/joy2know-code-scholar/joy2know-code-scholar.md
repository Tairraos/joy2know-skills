# joy2know-code-scholar 发布前检测报告

> 本文件是开发期文档，不进 zip（构建时自动排除）；但要进 git。规范见仓库根 `AGENT.md` 第 3–4 节。下次检测先读本文件，只重测有变化的部分与「未覆盖项」。
> 口径：每条结论对应一次真实运行（命令 + 样本 + 输出要点）。脚本实测跑在 `/tmp` 副本上（`packages/` 内不留 `__pycache__`，已确认干净）。

- **包名**：joy2know-code-scholar
- **类型**：专家（expertType: agent）
- **对应版本**：1.2.0
- **检测状态**：⚠️ 有条件通过
- **最后检测**：2026-09-23
- **检测方式**：四道门（AGENT.md §3）

---

## 一、包内文件（从磁盘枚举，不手写）

```
  .codebuddy-plugin/plugin.json   1940B   专家配置（expertType: agent）
  README.md                       2038B   包说明
  agents/joy2know-code-scholar.md 6397B   角色定义（6 条硬规则）
  skills.json                      192B   内嵌技能声明
  joy2know-code-scholar.md         本报告（开发期文档，不进 zip）
```

注：包内无 `scripts/`、无 `skills/` 目录、无 `avatars/` 目录。内嵌技能 `joy2know-codebase` 与头像均由构建期从仓库注入（见门2 / 门3）。

---

## 二、能力清单与验证结果

门1（功能测试）用「字段与结构真跑校验」代替跑脚本（本包纯文档包，无 scripts/）。核心校验器为仓库 `scripts/selfcheck.py`，真跑于 `/tmp` 副本。

| 能力 / 待验证项 | 验证方式（命令与样本） | 结果 | 证据要点 |
|---|---|---|---|
| plugin.json JSON 解析 + 字段遍历 | `selfcheck.py /tmp/j2k-cs/joy2know-code-scholar` | ✅ 通过 | 识别为 `专家包=joy2know-code-scholar`；`[OK] JSON 解析通过`、`[OK] 字段遍历完成` |
| displayDescription.zh 40–50 汉字 | 同上 | ✅ 通过 | 汉字 44 字（官方 40–50），实测计数 |
| defaultInitPrompt == quickPrompts[0] | 同上 | ✅ 通过 | 官方硬约束通过；zh「帮我搞清楚这个仓库是怎么回事」逐字相等 |
| tags / quickPrompts 各 3 条 | 同上 | ✅ 通过 | `tags 数量=3`、`quickPrompts 数量=3`（官方固定 3） |
| categoryId 合法（15 类内） | 同上 | ✅ 通过 | `categoryId 合法: 02-Engineering` |
| author.name 署名 | 同上 | ✅ 通过 | `署名 '晓得乐'` |
| agents 路径存在 | 同上 | ✅ 通过 | `./agents/joy2know-code-scholar.md` 存在 |
| agent name == agentName | 同上 | ✅ 通过 | `joy2know-code-scholar` 一致 |
| 包内无硬编码凭证 | 同上 | ✅ 通过 | `[sec] 未发现` |
| 内嵌技能声明存在 | 同上（`[!!]` 提示） | ⚠️ 提示 | `./skills/joy2know-codebase 尚未生成`——源目录本就不该有，构建时从 packages/ 复制；以构建暂存目录自检为准 |
| avatar 存在 | 同上（`[XX]` 问题） | ⚠️ 待确认 | `avatar 缺失: avatars/expert.png` —— 见门3：构建从仓库根 `avatars/joy2know-code-scholar.png` 注入，已确认该源文件存在（370761B），故上传不会因缺图失败 |
| 卡面标题/副标题口径（别放反） | 核对取值 | ✅ 通过 | `profession.zh=晓得·代码考古学家`（主标题）、`displayName.zh=晓得乐`（副标题），符合系列约定 |
| 反向验证（防放水） | 6 错样本 + 阴性对照 | ✅ 通过 | 见门4，全部被抓、合规样本不乱报 |

selfcheck 汇总：**通过 11 项 / 提示 2 条 / 问题 1 项**（avatar，已判定为非阻断）。

---

## 三、门2 完整性对照（文档承诺 → 实现）

README.md + plugin.json 的每条承诺，到 `agents/joy2know-code-scholar.md` 找落地证据（引原文行号）。

| 文档承诺（摘原文） | 实现位置（文件:行） | 有/无/部分 | 备注 |
|---|---|---|---|
| README「产出架构地图、入口链路、关键数据模型，以及一份『哪里能动、哪里是雷区』的改造风险清单」 | agents.md:14-15, :30-52（六节含改造风险清单 :50-52） | 有 | 六节结构逐节落地 |
| README「核心原则：不读全量，只读关键文件。用启发式扫描找出最重要的 20 个文件」 | agents.md:22-24，规则2 :64-71 | 有 | 20 文件默认 + 增量读取降级路径 |
| README「能回答的问题」（技术栈/入口/数据模型/雷区） | agents.md:9-12，六节 :35-52 | 有 | 与输出结构对应 |
| README「适用：接手遗留系统、技术尽调… 不适用：代码评审/重构实施/安全渗透」 | README:16-17；agents.md:110-114（边界） | 有 | 两处口径一致 |
| README「使用示例：帮我搞清楚这个仓库是怎么回事」 | README:22；plugin.json defaultInitPrompt；agents.md:22-27 | 有 | 与默认开场白逐字一致 |
| README「内置技能 joy2know-codebase：仓库扫描脚本（零第三方依赖）+考古方法论，自动跳过 node_modules/dist/vendor」 | agents.md:11（`skills:[joy2know-codebase]`）、:26（先跑扫描脚本）；plugin.json `skills` + skills.json | 部分 | 技能实体构建期注入（源包无 `skills/` 目录），其扫描脚本行为由 joy2know-codebase 自身检测覆盖；本包只校验「声明与定位一致」 |
| README「目录结构」列出 `avatars/expert.png` 与 `skills/joy2know-codebase/` | 源包实际无此两目录 | 部分 | 文档描述的是「构建产物」形态；两处均由构建注入。源包形态描述偏差（非阻断，见六·未覆盖 3） |
| plugin.json profession/displayName 口径 | plugin.json；系列约定 | 有 | profession=`晓得·代码考古学家` / `joy2know Codebase Archaeologist`；displayName=`晓得乐` / `joy2know`，完全符合 |
| plugin.json categoryId=02-Engineering、tags3、quickPrompts3、defaultInitPrompt=quickPrompts[0] | selfcheck 全绿 | 有 | 见第二节 |
| skills.json 声明 joy2know-codebase 与包定位匹配 | skills.json；plugin.json | 有 | 声明一致；技能即本包核心扫描能力 |

结论：承诺基本落地，无「文档写了、实现没做」的硬缺陷；2 处「部分」均属「构建注入 / 他包覆盖」，已在备注说明。

---

## 四、门3 合理性结论

- **profession 口径**：✅ 正确。`profession = 晓得·<职能>`（zh）/ `joy2know <Function>`（en），`displayName = 晓得乐`（zh）/ `joy2know`（en），与系列约定逐字吻合；卡面主标题取 profession、副标题取 displayName，未放反。
- **规则自洽**：✅ 无互相打架。六条硬规则（风险分级有据 / 不读全量 / 结论可溯源 file:line / 不猜未读 / 事实与判断分离 / 不改文件）取向一致；README 的「不适用」与 agents.md「边界」完全一致（代码评审、重构、渗透均「不主动做、仅提示」）。
- **推断是否标注**：✅ 规则4（防幻觉，优先级最高）明确要求未读推断标 `[推断]` 并给验证方法；规则1/2 的降级路径要求「依据不足」时显式声明。机制健全。
- **错误提示可指导下一步**：⚠️ 本包为纯文档/角色定义，无运行时脚本错误；其「降级路径」以文字规则形式给出（如扫描不足时「扩大范围到 N=40，是否继续？」），属对话层，未机测（见六）。
- **边界是否优雅**：✅ 角色边界清晰（只读分析、不重构、不渗透、超大仓库声明覆盖限制 agents.md:110-114），与「不猜 / 不擅自改」自洽。
- **零依赖是否守住**：⚠️ 本专家包自身零脚本零依赖；其价值依赖的内嵌技能 joy2know-codebase 自称「零第三方依赖」，但该脚本不在本包源内、未在此执行（见六·未覆盖 1）。
- **值来源是否可追溯**：✅ 规则3 强制每条架构/入口/数据模型断言附 `文件:行号`；规则5 强制「事实 vs 判断」分离——溯源与标注机制明确。
- **description 长度与系列齐平**：✅ **（2026-09-23 二审改判：原判「偏短」系口径误用）**
  实测本包 `plugin.json` 的 `description`(en) = **175 字符**，agent 正文 = **144 字符 / 94 汉字**。
  与**兄弟专家包**横向对照：`plugin.description` en → cine-team **176**、code-scholar **175**、mr **197**；
  agent 正文 → cine 主理人 195 / 成员 97–119、polish 主理人 192 / 成员 146–178。**本包在区间内，与兄弟包齐平。**
  误判根因见第六节「缺陷 2」：任务给出的「239–386 字」是**技能包 `SKILL.md` frontmatter 的 `description`** 的口径
  （实测技能包：clarify 239 / errorfix 386），**不是专家包的口径**。

---

## 五、门4 测试真实性与反向验证

**防放水七条自查**：①只跑 happy path？否（含 6 组反向错样本 + 阴性对照）；②只看没报错？否（核对每条 `[XX]`/`[OK]` 文本与取值，含汉字计数与逐字相等）；③把「文档写了」当「已验证」？否（门2 标 2 处「部分」并进未覆盖）；④自造理想样本？否（错样本为真实字段篡改，阴性对照为真实源包）；⑤跳过失败项？否（avatar 问题如实记录并定位为构建填充）；⑥修完只测修好的？不适用（本轮不改包内任何文件）；⑦只造会被抓的错？否（6 类错 + 1 阴性，见下）。

**反向验证记录**（全部跑在 `/tmp/j2k-cs` 副本，逐字引用 selfcheck 输出）：

| # | 造了什么错样本 | 预期 | 实际 |
|---|---|---|---|
| 1 | 删 plugin.json `author` | 报缺字段 | ✅ `[XX] plugin.json 缺字段: author` + `author 为空（官方必填）` |
| 2 | 删 `version` | 报缺字段 | ✅ `[XX] plugin.json 缺字段: version` |
| 3 | displayDescription.zh 改为 30 字 | 报过短 | ✅ `[XX] displayDescription.zh 汉字 30 字，超出官方要求 40-50` |
| 4 | displayDescription.zh 改为 60 字 | 报过长 | ✅ `[XX] displayDescription.zh 汉字 60 字，超出官方要求 40-50` |
| 5 | defaultInitPrompt.zh 末尾加「X」使其 ≠ quickPrompts[0] | 报不一致 | ✅ `[XX] defaultInitPrompt=…'帮我搞清楚这个仓库是怎么回事X'… 与 quickPrompts[0]=… 不一致，官方要求二者相同` |
| 6 | categoryId 改为 `00-ExpertTeam` | 报不在合法分类 | ✅ `[XX] categoryId 不在合法分类列表中: '00-ExpertTeam'（合法取值见官方文档「行业分类」，共 15 类）` |
| 7（阴性） | 原始合规包 | 不乱报 | ✅ 仅 1 条 `[XX]`（avatar，已知构建填充）；其它字段零漏报 |
| 8（阴性+） | 给副本补 `avatars/expert.png` 后重跑 | 问题清零 | ✅ `[XX] 总数=0`，全部通过——证明 selfcheck 基于文件存在性判定，非乱报 |

> 元发现：selfcheck 的 name/plugin 字段校验**对目录名敏感**。当副本误命名为 `code-scholar`（≠包名）时，会多报 2 条假 `[XX]`（name/plugin 不一致）；用正确目录名 `joy2know-code-scholar` 重跑后该 2 条消失。本报告结论均基于正确目录名副本，结论可靠。

**一句话回答**：本包若坏掉，哪步亮红灯？→ plugin.json 任一必填缺失 / 长度越界 / 初始化提示不一致 / 分类非法，都会在 selfcheck 对应 `[XX]` 亮红灯（6 组反向验证已证实）。唯一非阻断红灯是 avatar 缺失，但构建会从 `avatars/joy2know-code-scholar.png` 注入，上传不会失败。

---

## 六、未覆盖项与缺陷

**未覆盖项**（下次检测优先消掉）：

1. **内嵌扫描脚本行为未在本包实测**：核心能力（启发式打分定位 20 文件、跳过 node_modules/dist/vendor、依赖清单）属于 joy2know-codebase 技能，源包不含、构建注入；本包只校验「声明与定位一致」，未执行其扫描脚本。其正确性由 joy2know-codebase 自身检测覆盖。
2. **AI 行为层未测（不可机器测）**：六节报告质量、风险分级的「可验证依据」是否真附引用、`[推断]` 标注纪律、事实/判断分离——均在对话层，本地无 agent 运行时，未跑真实端到端考古对话。
3. ~~**README 目录树形态偏差**~~ **【已消 2026-09-23】**：README 已补「**形态说明**」注明该目录树为**构建后所得包的形态**，
   并写明源包只有哪几个文件、`avatars/` 与 `skills/` 分别由构建从何处注入。
4. **真仓库扫描召回/精度未验证**：「20 文件启发式」对真实大型仓库的召回率与噪音剔除效果，依赖内嵌技能，未在本包验证（见 1）。
5. **多语言卡面渲染未实测**：profession(主)/displayName(副) 在客户端实际渲染效果仅按惯例核对取值，未真机渲染。
6. ~~**description 长度低于系列齐平区间**~~ **【已消 2026-09-23 · 改判为口径误用】**：实测本包 175 字符，
   与兄弟专家包（176 / 197）齐平，**不构成缺陷**；「239–386」属技能包口径。详见第六节「缺陷 2」的复核记录。

**已知缺陷**（**处理情况见本节末**）：

- **【已修 2026-09-23】** 缺陷 1（轻·源包形态）· README 目录树列了构建注入的文件：见未覆盖 3，非阻断，已加注「源包形态，图标与技能由构建注入」。
- **【已改判 2026-09-23 · 系口径误用，不构成缺陷】** 缺陷 2（轻）· description「偏短」：见未覆盖 6。
**【2026-09-23 处理记录 · 版本 1.1.0 → 1.2.0】**

- **缺陷 1 已修**：`README.md` 的「目录结构」块加注 `（由构建注入）`，并在块后补一段「**形态说明**」——
  写明该目录树为**构建后所得包的形态**；仓库里的**源包**只有 `.codebuddy-plugin/plugin.json`、`agents/`、
  `skills.json` 与 `README.md`；`avatars/` 由构建从仓库根注入、`skills/` 按 `skills.json` 声明的名单从
  `packages/<技能名>/` 复制**唯一真源**（本包不存副本，避免同一技能两份、改一处漏一处）。
- **缺陷 2 【改判为「口径误用」，不改 description】**：报告原先写「`description` 偏短，低于 239–386 字区间」，
  本轮**独立复核推翻该判定**：
  - 「**239–386 字**」这一区间在 `docs/平台资产规范.md` 里位于 **「SKILL.md frontmatter —— 9 个必填」** 节下的
    「`description` 是产品本身」小节 → 它约束的是**技能包**的 `description`。
    用 YAML 正式解析 14 个技能包实测：min = **239**（clarify）、max = **386**（errorfix）、中位 364 —— 与区间完全吻合。
  - **专家包**的 `plugin.json` `description`(en) 实测兄弟包为 **176 / 175 / 197**，均**远低于** 239。
    本包 175 与兄弟包齐平，**不是「偏短」**。
  - **两套口径会给出相反结论**，已用探针脚本固化（见下验收表 D5）：同一三个包在技能包口径下**全部被判「偏短」**，
    在专家包口径下**全部通过** —— 即：**误用该区间会产生 3 条并不存在的缺陷**。
  - 因此**不改** `description`（改了反而把它推离兄弟包）。同步把门3 那条与未覆盖项 6 一并改判。
    另：本包 `displayDescription.en` = **220 字符**，兄弟包 218–222，**逐包齐平**，无需动。

**验收（真跑；改后 5/5，git 原始版本 4/5）**：

| # | 用例 | 改前（git 原始版本） | 改后 |
|---|---|---|---|
| D1 | 能定位 README「## 目录结构」下的代码块 | ✅ | ✅ |
| D1b | 目录树含 `avatars`/`skills/` 时必须加形态说明 | ❌ `注入项=['avatars','skills/']；说明=无` | ✅ 说明已加 |
| D2 | `plugin.description`(en) 落在**专家包**口径 170–205 | ✅ 175（本来就在） | ✅ 未变 |
| D3 | `displayDescription.en` 落在系列口径 210–230 | ✅ 220 | ✅ 未变 |
| D4 | `displayDescription.zh` 40–50 汉字 | ✅ 44 | ✅ 未变 |
| D5 | **口径探针**：四个专家包在两种口径下的判定表 | — | ✅ 3 条判据全过（见下） |

**口径探针实测表**（`probe_caliber.py`，判据同时含「正向」与「反向」两个方向）：

| 包 | desc 长度 | 技能包口径 239–386 判 | 专家包口径 170–205 判 |
|---|---|---|---|
| cine-team | 176 | **偏短**（误报） | 通过 |
| code-scholar | 175 | **偏短**（误报） | 通过 |
| mr | 197 | **偏短**（误报） | 通过 |
| polish-team | 502 | 偏长 | **偏长**（真偏离，两套口径一致） |

探针 3 条判据：①两套口径结论不同的包数 > 0（实测 3）；②另三包在专家包口径下**全部通过**；
③同三包在技能包口径下**全部被判「偏短」**。三条同时成立 = 「该区间误用于专家包会产生 3 条不存在缺陷」。

**反向验证**：同一套用例对 **git 原始版本**跑出 **4/5**，唯一失败项 D1b 正是缺陷 1 原文；
**D2/D3/D4 在两侧都通过，这是正确结果** —— 本轮**没有**改 description（它本就齐平），
所以「通过」不是靠改数据换来的，而是靠**换对判据**。这正是口径纠正与数据修正在验收上的区别。

**校验器自身修了 1 处判据（值得记）**：D1 初版用 `re.search(r"```\n.*?```", rt, re.S)` 取「全文第一个代码块」，
而 README 第一个块是**「使用示例」**，里面没有 `avatars/skills`，于是 `注入项=[]` → **假通过**（对原版也报绿）。
已改为先定位 `^##+ 目录结构` 小节、再取其中代码块。**「没定位到目标 → 判为通过」是最危险的假绿。**



> **严重度判定**：上述均**不影响上架解析**（avatar 与技能均由构建注入且源文件已确认存在）、**无数据错误**、**无危险行为**（角色规则明确「只读、不改文件、不渗透」）。故判为「非硬缺陷」，状态取 `⚠️ 有条件通过`。

**待办**（缺陷 1、2 已于 2026-09-23 处理，见上）：

- ~~修缺陷 1（README 加构建注入注记）~~ → **已修**。
- ~~缺陷 2（补 description）~~ → **改判：不补**（原判定系口径误用，本包与兄弟包齐平；已用探针脚本固化口径）。
- 下次检测优先补未覆盖 2（跑一次真实考古对话，核风险依据与 `[推断]` 纪律）、未覆盖 4（用真实仓库验扫描召回）。
- 修完跑回归：selfcheck 在正确目录名副本重跑 + 门4 六组反向验证复跑。
- **给 `docs/平台资产规范.md` 的后续建议**：把「239–386」明确标注为**技能包**口径，并补专家包口径（
  `plugin.description` en 170–205、`displayDescription.en` 210–230）—— 否则同一误用会在下一个专家包重现。
  **本轮未改该文档**（属跨包规范，另案处理）。

---

## 七、检测履历

| 日期 | 版本 | 状态 | 摘要（本轮测了什么、改了什么） |
|---|---|---|---|
| 2026-09-23 | 1.1.0 | ⚠️ 有条件通过 | 首次成文（覆写骨架）。在 `/tmp` 副本真跑 `scripts/selfcheck.py`：识别为专家包，11 通过 / 2 提示 / 1 问题（avatar，已判定为构建填充，源文件 `avatars/joy2know-code-scholar.png` 存在）。6 组反向验证全抓（删 author/version、displayDescription.zh 30/60 字、defaultInitPrompt 改字、categoryId=00-ExpertTeam）+ 2 组阴性对照（合规包零字段漏报、补 avatar 后全绿）。门2 承诺逐条落地（2 处「部分」属构建注入/他包覆盖）；门3 profession 口径正确、规则自洽、无矛盾。未覆盖 6 项，含 AI 行为层与扫描脚本行为（依赖内嵌技能）。**未改包内任何文件，`packages/` 下无 `__pycache__`。** |
| 2026-09-23 | 1.2.0 | ⚠️ 有条件通过 | **修复轮**：缺陷 1 → README「目录结构」加注「由构建注入」+ 补「形态说明」段（写明源包实际文件与注入来源）；缺陷 2 → **二审改判为「口径误用」，不改 description**：实测本包 plugin.description(en)=175，兄弟包 176/197 齐平；「239–386」经 YAML 解析 14 个技能包实测确认为**技能包**口径（min 239 clarify / max 386 errorfix）。新增口径探针脚本，证明该区间误用于专家包会产生 3 条不存在缺陷。验收：**真跑 5/5（含探针 3 条），同一套用例对 git 原始版本 4/5**（唯一失败 D1b 即缺陷 1；D2/D3/D4 两侧均通过 = 口径纠正而非改数据）。校验器 D1 初版曾假通过，已修。 |
