# 发布前检测报告 · 晓得·凡人腔调（joy2know-humanize）

> **本文件是开发期文档，不进 zip**（构建时自动排除）；但**要进 git**。
> 规范见仓库根 `AGENT.md` 第 3–4 节。**下次检测先读本文件**，只重测有变化的部分与「未覆盖项」。

- **包名**：joy2know-humanize
- **类型**：技能
- **对应版本**：1.1.0
- **检测状态**：⛔ 未检测
- **最后检测**：—
- **检测方式**：—

---

## 一、包内文件（从磁盘枚举，不手写）

```
  SKILL.md
  references/baseline.md
  references/not-rules.md
  references/rules.md
  scripts/fix.py
  scripts/guard.py
  scripts/rules.py
  scripts/scan.py
  scripts/selftest.py
```

## 二、能力清单与验证结果

> 「结果」栏取值：`✅ 通过` / `❌ 失败` / `⚠️ 部分` / `未覆盖`。
> **每格都要有证据**：真实命令 + 真实输出要点。只写「没报错」不算验证。

| 能力 / 待验证项 | 验证方式（命令与样本） | 结果 | 证据要点 |
|---|---|---|---|
| 脚本 `scripts/fix.py` 可运行且行为与 SKILL.md 描述一致 | `--help` 至少一次 · 主流程真跑一次 · 边界样本一次 | 待测 | — |
| 脚本 `scripts/guard.py` 可运行且行为与 SKILL.md 描述一致 | `--help` 至少一次 · 主流程真跑一次 · 边界样本一次 | 待测 | — |
| 脚本 `scripts/rules.py` 可运行且行为与 SKILL.md 描述一致 | `--help` 至少一次 · 主流程真跑一次 · 边界样本一次 | 待测 | — |
| 脚本 `scripts/scan.py` 可运行且行为与 SKILL.md 描述一致 | `--help` 至少一次 · 主流程真跑一次 · 边界样本一次 | 待测 | — |
| 脚本 `scripts/selftest.py` 可运行且行为与 SKILL.md 描述一致 | `--help` 至少一次 · 主流程真跑一次 · 边界样本一次 | 待测 | — |
| 参考文档 `references/baseline.md` 所述与实现一致（不能只是"写得好听"） | 按文档描述的格式造样本，核对实现是否真按此处理 | 待测 | — |
| 参考文档 `references/not-rules.md` 所述与实现一致（不能只是"写得好听"） | 按文档描述的格式造样本，核对实现是否真按此处理 | 待测 | — |
| 参考文档 `references/rules.md` 所述与实现一致（不能只是"写得好听"） | 按文档描述的格式造样本，核对实现是否真按此处理 | 待测 | — |
| `SKILL.md` 声明的每条能力都有对应实现（门 2 完整性） | 逐条摘出承诺 → 与实现对照 → 标 有/无/部分 | 待测 | — |
| `description` 里「不适用于」的场景确实不触发 | 造该场景的输入，确认不会误触发 | 待测 | — |
| SKILL.md frontmatter 9 个必填字段齐全且 YAML 合法 | `python3 scripts/selfcheck.py packages/joy2know-humanize` | 待测 | — |
| 降级路径：声明「缺 X 就降级」的，把 X 藏起来仍不崩且提示正确 | 临时改名/移走依赖后重跑 | 待测 | — |
| 反向验证：故意造「应该被抓」的错样本，确认真的被抓 | 至少 1 组（例如去掉一个必填字段、篡改一处内容） | 待测 | — |

## 三、门 2 · 完整性对照（文档承诺 → 实现）

> 把 `SKILL.md` / `plugin.json` / `agents/*.md` 里的能力承诺逐条摘出来对照。
> 重点抓「文档写了、实现没做」——这在 v1.1.0 的 `joy2know-asset-ledger` 上真实发生过。

| 文档承诺（摘原文） | 实现位置 | 有 / 无 / 部分 | 备注 |
|---|---|---|---|
|  |  |  |  |

## 四、门 3 · 合理性结论

- **规则是否自洽**：（两条规则有没有互相打架）
- **推断是否标注为推断**：（靠启发式猜出的结论有没有标出来）
- **错误提示是否可指导下一步**：
- **边界是否优雅**（空数据 / 缺文件 / 旧格式 / 重复记录）：
- **零依赖是否守住**：
- **值来源是否可追溯**：

## 五、门 4 · 测试真实性自评（防放水）

> 逐条自查下面七种假测试形态，中一条即视为本门未通过。

- [ ] 1. 只跑 happy path，没跑边界
- [ ] 2. 只看「没报错 / 退出码 0」，没核对输出内容
- [ ] 3. 把「文档写了」当成「已验证」
- [ ] 4. 自造理想样本（比真实产物干净）
- [ ] 5. 跳过失败项、不记录
- [ ] 6. 修完只测修好的那条，没跑回归
- [ ] 7. 造错样本只造了会被抓的那种（门禁变橡皮章）

**反向验证记录**（做了什么、造了什么错样本、工具是否亮红灯）：

**一句话回答**：本包的核心能力如果坏掉，是哪一步会亮红灯？

## 六、未覆盖项 / 已知缺陷 / 待办

> **这一栏不许空着。** 没有任何一次检测能覆盖 100%；空着 = 没认真写。
> 每次检测顺手消掉一条，让它越来越薄。

**未覆盖项**（尚未验证的能力或场景）：

- 本报告尚未做任何实测 —— **全部能力均为未覆盖**。按「二」的待验证项逐条走完四道门后回填。

**已知缺陷**：

- （无记录）

**待办**：

- 完成首次四道门检测，把头部「检测状态」改为实际结论，并把「对应版本」锁定为当时版本。

## 七、检测履历

| 日期 | 版本 | 状态 | 摘要（本轮测了什么、改了什么） |
|---|---|---|---|
| — | 1.1.0 | ⛔ 未检测 | 骨架生成，等待首次检测 |
