# AGENT.md — 本仓库的工作约定

> 这份文件是**给 AI 助手（Harness）和人的操作手册**。规则只写一遍，改先改这里。
> 仓库 = 「晓得 / joy2know」WorkBuddy 开放平台资产包（Skill / Expert / Expert Team）。
> 远端：`git@github.com:Tairraos/joy2know-skills.git`，主分支 `main`。

---

## 0. 权威文件索引（先读这里，别猜）

| 想干什么 | 读哪个 |
|---|---|
| **走一遍发布流程、过发布前检测** | 本文件 |
| **查平台的字段 / 结构 / 体积 / 类目 / 许可口径** | `docs/平台资产规范.md` |
| 看全部包的当前版本与状态 | `README.md` 的「资产总表」；实时状态用 `pnpm run list` |
| 建议发布类目、平台侧最后版本 | `release.config.json` ——**唯一真源** |
| 待上传的产物、发布清单、发布历史 | 产物在 `dist/`；**清单与发布历史在仓库根的 `发布清单.md`**（前四节自动生成，末两节人工维护） |
| 字段级自检的规则实现 | `scripts/selfcheck.py`（**本仓库这一份是权威版本**） |
| 某个包上次检测到哪一步 | `packages/<包名>/<包名>.md` |

**本仓库自给自足**：自检脚本、字段规范、打包工具都在仓库里，不依赖任何本机技能目录
（`scripts/selfcheck.py` 于 2026-09-23 从个人技能搬入，`docs/平台资产规范.md` 同源）。
个人技能 `WorkBuddy资产包生产` 里的那份是通用版本；**规则冲突时以本仓库为准**。

---

## 1. 铁律（改包前先看这八条）

1. **凡需英文名的位置，「晓得」一律译作 `joy2know`**
   （技能 `display_name_en: joy2know <Name>`；专家 `profession.en` / `displayName.en`；包名 `joy2know-xxx`）。
   母品牌「晓得」，口号「一目晓得」，署名 **`Tairraos - 晓得乐`**。
2. **`name` 不参与触发，`description` 才是产品。** 改 `name` 对触发零影响。
3. **改内容就动版本号。** 口径：实质内容改动 → patch；新增能力 / 规则 / 角色 → minor；字段或包结构改动 → major。
4. **一个资产包一条提交**，注释用五段式（见第 7 节）。
5. **同一份规则只允许存在一份。** 源包里不放技能副本、不放图标 —— 内嵌技能由包根 `skills.json`
   声明、构建时从 `packages/<技能名>/` 复制；图标只在仓库根 `avatars/`。
6. **不要在 `dist/` 里改东西。** 那是产物目录，改源包重跑构建。
7. **类目问题不抬版本号。** 平台因「所选类目不符」打回是后台操作问题，改类目重提即可；见 `docs/平台资产规范.md` 第八、十一节。
8. **每次涉及发布，都要更新仓库根的 `发布清单.md`。** 只要本轮有「包内容变了」或「真去平台传了」，
   收工前必须动这份文档 —— 具体动作见第 2 节第 ⑦ 步。**这是 AI 的活，不是等人的活**：
   忘掉它等于发布历史断档，下一个人（或下一个 harness）会照着过期清单上传错版本。

---

## 2. 发布流程（严格版）

新增技能时先做**归置判断**（该不该做成专家 / 团）——见 `docs/平台资产规范.md` 第十节。

```
① 改动源包           只在 packages/ 改。不要手工改 dist/。
② 升版本号           改到哪一级按第 1 节口径。版本落点：技能 = SKILL.md 的 version；
                    专家/团 = plugin.json 的 version。（技能包不含包内 README）
③ 【发布前检测】      ★ 强制门：过第 3 节的四道门。没过不许打包。
④ 写检测报告          把结果写进 packages/<包名>/<包名>.md（第 4 节）。
⑤ 构建               pnpm build <包名>      → 产物 dist/<包名>-v<版本>.zip
⑥ 提交               五段式注释，一个包一条提交。
⑦ 发布记账 ★          AI 必须动 发布清单.md（见下）。不记账视为本轮未收工。
```

**⛔ 硬门禁：第 ③ 步没过，不许进第 ⑤ 步。**
「我调用 AI 打包」＝ AI 必须先做发布前检测。构造上的证据就是：
`packages/<包名>/<包名>.md` 里**有本轮日期、对应版本号等于当前包版本、且状态不是「未检测」**。
构建时若发现报告缺失 / 未通过 / 版本不符 / 比源码旧，会**明确告警**；加 `--strict` 则直接失败。

### 第 ⑦ 步：发布记账 —— 每次都要动 `发布清单.md`

**清单在仓库根**（`发布清单.md`）。它的数据来源分两半，**别改错地方**：

| 想改什么 | 改哪里 |
|---|---|
| 前四节的表格（版本 / 体积 / 包名 / 状态 / 建议类目） | **不要手改** —— 由 `pnpm build` 按实际版本 + `release.config.json` 重写 |
| 某个包的建议发布类目、是否已实证 | `release.config.json` 的 `suggestedCategory` / `categoryConfirmed` |
| 平台侧最后版本与备注 | `release.config.json` 的 `platform.version` / `platform.note` |
| 「五、发布时要注意」 | 直接编辑 `发布清单.md`（构建不会动） |
| 「六、发布历史」 | 直接编辑 `发布清单.md`（构建不会动） |

**AI 的固定动作（本轮只要沾了发布，逐条做掉）**：

1. **包内容变了** → 已升版本号 → 重跑 `pnpm build`（前四节自动更新，含体积与【本地升级】标记）。
2. **真去平台传了 / 被平台打回** → 让用户提供结果，然后：
   - 更新 `release.config.json` 里该包的 `platform.version`（与 `note` 若有必要）；
   - 在 `发布清单.md`「六、发布历史」**追加一行**：日期 · 包名 · 类型 · 版本 · 上传文件 · 结果；
   - 若平台给出了类目判定，回填 `release.config.json` 的 `suggestedCategory` + `categoryConfirmed: true`。
3. **被打回** → 「五、发布时要注意」里追加一条**可执行的下一步**（改哪里、传哪个文件、选什么），
   不要只写「被打回了」。
4. **日期口径**：不知道确切日期就写「日期未记录」，**不许编日期**。文件按当时的**实际产物名**写，
   早期无版本号的照原样写，**不追改历史**。

---

## 3. 发布前检测：四道门

**检测不是「跑一遍没报错」。** 四道门依次过，每道门都要留下**可复核的证据**（命令 + 样本 + 输出要点）。

### 门 1 · 功能测试（真跑）

- **每条对外能力都要真跑一次**：每个子命令 / 每个主流程，给真实命令与真实输出。
- **每条能力至少配两个样本**：一个正常样本、一个边界样本
  （空输入 / 缺文件 / 空目录 / 中文与英文混排 / 超长 / 不合规格式）。
- **降级路径要单独测**：声明「缺 X 就降级」的，把 X 藏起来再跑一次，确认不崩且提示正确。
- 禁止用「读代码推断它能跑」代替运行。**没跑过的，一律记入「未覆盖项」。**

### 门 2 · 功能完整性（对照承诺）

- 把 `SKILL.md` / `plugin.json` / `agent` 文件里的**能力承诺逐条摘出来**，与实现对照，
  逐条标 **有 / 无 / 部分**。
- 重点抓**「文档写了、实现没做」**：这是本仓库真实发生过的一类事故
  （`joy2know-asset-ledger` v1.1.0 有三条硬承诺，脚本一条没兑现、两条做反了）。
- 对照`description` 里写的触发场景与「不适用于」边界：**声明不适用的场景，实现里必须真的不触发。**
- 兜底口径：**文档写了的能力，实现里必须有；实现里有的能力，文档里必须写。**

### 门 3 · 合理性

- **规则自洽**：两条规则不能互相打架。本仓库真实案例：一条红线写「禁止按修改时间检索」，
  另一处又要「按时间邻近推断批次」—— 必须给出判断标准（**推断线索**可以吃文件属性，
  **检索维度**不可以），否则使用者无从下手。
- **推断必须标推断**：靠启发式猜出来的结论（如批次、角色）要显式标记，不能当事实写。
- **错误提示要能指导下一步**：报错要说明「发生了什么 + 你可以怎么做」，不能只抛堆栈。
- **边界要优雅**：空数据、缺文件、旧格式、重复记录 —— 要给出提示而不是崩或静默。
- **零依赖是否守住**：声明「零第三方依赖」的，检查是否真的没有 import 外部包。
- **值来源可追溯**：取自外部的值要记来源（如 `embedded` / `sidecar` / `user` / `none`），缺就标「未提供」，**不许凭空补值**。

### 门 4 · 测试真实性（防放水）★ 最容易糊弄的一道

**假测试的七种形态 —— 逐条自查，中一条即视为未通过：**

| # | 假测试形态 | 怎么破 |
|---|---|---|
| 1 | 只跑 happy path，不跑边界 | 每条能力必须有边界样本（门 1） |
| 2 | 只看「没报错 / 退出码 0」，不看输出内容对不对 | 必须核对输出内容本身，而不是只看它跑完了 |
| 3 | 把「文档写了」当成「已验证」 | 证据必须是**运行的输出**，不是文档的复述 |
| 4 | 自造理想样本（比真实产物干净） | 样本必须贴近真实格式；能拿到真实产物就用真实的 |
| 5 | 跳过失败项、不记录 | 失败与未测项必须写进「未覆盖项」，藏起来等于造假 |
| 6 | 修完只测修好的那条，不测原来对的那几条 | 必须跑**回归**：原有能力全部再跑一遍 |
| 7 | 造错样本只造了会被抓的那种（门禁变橡皮章） | 必须做**反向验证** |

**反向验证（必修课）**：修校验器 / 修 bug 之后，**故意造一批「应该被抓」的错样本**，
确认工具确实报错。否则可能只是把门禁放水成了橡皮章。
（本仓库真实案例：把「专家团成员名不一致」的误报消掉后，必须造一个「既非 `agentName`
也不在 `members[].id`」的名字，确认仍然红灯。）

**放水的信号灯**（出现任一，报告即视为不合格）：

- 「未覆盖项」一栏是空的 —— 没有任何一次检测能覆盖 100%，**空 = 没认真写**。
- 证据里只有命令没有输出要点，或只有结论没有样本。
- 测试样本由检测者自己编造且明显比真实格式简单。
- 结论写「应该没问题 / 大概率可以」。

**四道门跑完，报告里必须能回答一句话**：
**「这条能力如果坏掉，是哪一步会亮红灯？」** 答不上来的能力，就是没人看着的能力。

---

## 4. 检测报告：`packages/<包名>/<包名>.md`

**位置与命名**：与包目录同名，放在包目录内 —— `packages/joy2know-asset-ledger/joy2know-asset-ledger.md`。

**它不进 zip**（构建时自动排除，是纯开发期文档）；**它进 git**（要能看历史）。

### 头部必须有的四行（构建脚本靠它判门禁，格式不要改）

```md
- **对应版本**：1.2.0
- **检测状态**：✅ 已通过        ← 取值见下表，只能四选一
- **最后检测**：2026-09-23
- **检测方式**：功能测试 + 完整性对照 + 合理性检查 + 反向验证
```

**状态的语义（构建脚本按此判门禁）**：

| 状态 | 含义 | 能否打包 |
|---|---|---|
| `✅ 已通过` | 四道门全过，无遗留未覆盖项 | ✅ 可以 |
| `⚠️ 有条件通过` | 四道门已过，但**存在明确的未覆盖项**（必须列在第六节） | ✅ 可以，但未覆盖项要写清 |
| `⛔ 未检测` | 还没测，或只生成了骨架 | ❌ 构建会告警；`--strict` 直接失败 |
| `❌ 未通过` | 发现有硬缺陷，未修完 | ❌ 构建会告警；`--strict` 直接失败 |

**只允许四选一。** 写「大概没问题」「基本通过」这类模糊状态，等同于 `⛔ 未检测`。

### 正文结构

```
一、包内文件            （从磁盘枚举，不手写）
二、能力清单与验证结果   表：能力 | 验证方式（命令） | 样本 | 结果 | 日期
三、门 2 完整性对照      表：文档承诺 | 实现位置 | 有/无/部分
四、门 3 合理性结论
五、门 4 测试真实性自评  含「反向验证做了什么」
六、未覆盖项 / 已知缺陷 / 待办   ← 不许空着
七、检测履历             表：日期 | 版本 | 状态 | 摘要
```

### 复用规则（这是写这份文档的全部意义）

**下次检测先读这份文档，不要从头再测一遍。** 具体做法：

1. **可以信任**：文档里「已验证 + 对应版本未变 + 相关文件未改」的能力。
   （构建会检查报告是否比源码旧：`packages/<包名>/` 里有文件比报告新，就告警「报告可能已过期」。）
2. **必须重测**：本次改动碰到的能力 + 它影响到的相邻能力（回归）+ 文档里列出的「未覆盖项」。
3. **顺手补漏**：每次至少消掉一条「未覆盖项」，让它越来越薄。
4. **每次检测都要追加「七、检测履历」一行**，并更新头部四行（尤其是「对应版本」）。

> 一句话：**报告是给下一次的自己省时间用的，不是给这一次的自己交差用的。**

---

## 5. 命令速查

```bash
# —— 打包（会顺带跑字段自检 + 重写发布清单）——
pnpm build                        # 全部包
pnpm build joy2know-asset-ledger  # 单个包（支持前缀匹配）
pnpm run list                     # 列出全部包 + 版本 + 图标状态 ← 注意是 run list，裸 pnpm list 会被截走

# —— 只做字段级自检（不打包）——
V=~/.workbuddy/binaries/python/envs/default
python3 scripts/selfcheck.py packages/joy2know-asset-ledger
#   ↑ 若报「PyYAML 不可用」说明静默降级成正则了，用 $V/bin/python 跑：
$V/bin/python scripts/selfcheck.py packages/joy2know-asset-ledger

# —— 生成缺失的检测报告骨架 ——
node scripts/init-checkdocs.mjs

# —— 图标 ——
python3 scripts/placeholders.py --check   # 缺哪些 / 哪些还是占位图
python3 scripts/placeholders.py           # 补占位图（只补不覆盖）
python3 scripts/shrink-icons.py           # 体检规格（--apply 才替换，先备份）
sips -g pixelWidth -g pixelHeight avatars/<名>.png   # 查单张规格
```

**自检跑在哪：**
- **正式门禁跑在构建的暂存目录上**（`pnpm build` 已经这么做，且**优先用仓库自带的 `scripts/selfcheck.py`**）。
  理由是内嵌技能与头像都是**构建时才注入**的，只有暂存目录里才是「真实交付的样子」。
- 想**单独跑**也可以（`python3 scripts/selfcheck.py packages/<包名>`）。对**源目录**跑时，
  「内嵌技能尚未生成」「包内头像尚未注入」会记成**提示**而不是失败 —— 因为那本来就是构建的活。
- **一个包都没识别到时会明确报错**（退出码非 0），不会输出「全部自检通过」。
  「什么都没查」报绿是比报错更危险的事，这条是踩过才知道要防的。

**`pnpm build` 的产物与副作用**（要知道它会动什么）：

- 写出 `dist/<包名>-v<版本>.zip`；**自动清理 dist/ 里同包的旧版本 zip**（`--keep-old-zips` 可关）。
- 重写**仓库根 `发布清单.md`** 的前四节；**`<!-- AUTO:END -->` 之后的人工段落原样保留**。
- 顺手清理 `dist/` 里遗留的旧位置清单（曾放在 dist/ 下），避免两处并存看错一份。
- 不改源包、不改 `release.config.json`。

---

## 6. 收工清单（每轮交付都要出）

输出**全量**技能 / 专家 / 专家团清单，**带版本号**；本轮改过版本的标【本地升级】
（= 版本号已抬高但尚未上传平台）。清单落在 `README.md` 的资产总表里，不要只在对话里说一遍。

---

## 7. 提交约定

**一个资产包一条提交**，注释五段式：

```
feat(skill|expert|team|fix|docs|chore): 新增<类型> <中文名> · <包名> v<版本>

一句话定位。

设计要点：
- 这条包最不好自己悟出来的设计决定，2–4 条

包内文件（n）：
  SKILL.md
  references/xxx.md

图标：avatars/<名>.png（512×512 PNG，nnn KB）
```

- **文件清单从磁盘枚举，不手写** —— 手写必然与实物不符。
- 图标跟包一起提交，保证每条提交自洽可检出。
- 批量生成提交注释要**先打样再批量跑**。
- **同一文件的多个修改不要并行发 Edit**（会互相覆盖且都报成功），串行改或用脚本一次改完。
- **推送由人来做，AI 不管远程。** 不要执行 `git push`，**也不要检查 / 汇报本地与远端的同步状态**
  （`git status` 里领先几个提交、要不要推 —— 都不是 AI 该操心的事）。AI 的边界是**把提交做在本地**。

---

## 8. 红线（禁止事项）

- ❌ 不核实就说「平台规则是怎样的」—— 字段与取值一律回 `docs/平台资产规范.md`；
  **模板里抄来的具体取值不算核实**（已连续踩两次，见该文件第六节）。
- ❌ 把「文档写了」当成「已验证」；把「没报错」当成「功能对」。
- ❌ 为了让报告好看而跳过失败项，或把「未覆盖项」留空。
- ❌ 改了能力却不抬版本号；或在 `dist/` 里直接改产物。
- ❌ 在包里放技能副本 / 图标副本（漂移的温床）。
- ❌ 未过第 3 节四道门就打包上传。
- ❌ 沾了发布却不动 `发布清单.md` / `release.config.json`（发布历史断档）。
- ❌ 执行 `git push`，或去核对本地与远端的同步状态（推送是人的事，见第 7 节）。
- ❌ 对 `dist/` 之外的目录做删除类操作前必须先列出路径并确认。
